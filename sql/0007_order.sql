-- ═══════════════════════════════════════════════════════════════
-- Re:Bind — 작업의뢰서 사진
--
-- 촬영해서 만든 프로젝트에는 원본 사진을 붙여 둡니다.
-- 나중에 "몇 부라고 했더라" 를 대조할 때 씁니다.
--
-- 완성본 사진(photos)과 따로 두는 이유:
-- photos 는 거래명세서와 고객사 화면에 나갑니다.
-- 작업의뢰서는 우리 쪽 원본이라 거기 끼면 안 됩니다.
-- ═══════════════════════════════════════════════════════════════

alter table public.projects
  add column if not exists order_photo text;
