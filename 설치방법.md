# Re:Bind 설치 — 10분

Re:Call 이 이미 깔려 있다는 전제입니다. 계정과 회사는 그것을 그대로 씁니다.
새로 할 일은 **표 만들기 한 번**, **서버 함수 배포 한 번**, 이 둘뿐입니다.

접속 정보는 Re:Call 저장소의 `진행상황.md` 에 있습니다.

```
Project ref : izrtclsqhsgkuwsffifn
Project URL : https://izrtclsqhsgkuwsffifn.supabase.co
```

---

## 1. 표 만들기 (한 번)

[SQL 편집기](https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn/sql/new)에
`sql/0003_works.sql` 을 통째로 붙여넣고 실행합니다.

생기는 것:

| | |
|---|---|
| `projects` | 프로젝트 하나 = 한 줄 |
| `project_steps` | 공정 기록 |
| `company_settings` | 명세서에 찍히는 우리 회사 정보 (회사마다 한 줄) |
| `works` 보관함 | 완성본·공정 사진. 경로가 `{회사id}/{프로젝트id}/…` 라 폴더 이름이 곧 권한입니다 |

Re:Call 의 표는 건드리지 않습니다. 실행해도 고객 자료는 그대로입니다.

> `supabase db push` 는 쓰지 마세요. 이 폴더에는 0001·0002 가 없어서 CLI 가 헷갈립니다.
> 그래서 `migrations` 가 아니라 `sql` 폴더에 두었습니다. 붙여넣기로 한 번만 실행하면 됩니다.

## 2. 서버 함수 배포 (한 번)

고객사는 계정이 없습니다. 그래서 링크에 든 열쇠를 확인하고 **그 프로젝트 하나만**
꺼내 주는 창구를 따로 둡니다. 데이터베이스는 로그인하지 않은 사람에게
아무 권한도 주지 않습니다.

```bash
cd /Users/motodna/Desktop/Rebind
supabase functions deploy share-view --no-verify-jwt --project-ref izrtclsqhsgkuwsffifn
```

`--no-verify-jwt` 를 빠뜨리면 고객사가 링크를 열 수 없습니다. 꼭 넣으세요.

## 3. 앱을 올릴 곳 정하기

`bindery.html` 하나만 올리면 됩니다.
Re:Call 과 같은 곳(깃허브 페이지)에 두는 것이 간단합니다.

올리는 주소가 정해지면 서버 함수가 그 주소를 받아들이도록 열어 줍니다.

```bash
supabase secrets set ALLOWED_ORIGIN="https://rebind.dnalabs.kr,https://recall.dnalabs.kr" \
  --project-ref izrtclsqhsgkuwsffifn
supabase functions deploy share-view --no-verify-jwt --project-ref izrtclsqhsgkuwsffifn
```

값을 바꾼 뒤에는 **함수를 다시 배포해야** 반영됩니다.
테스트가 끝나면 `localhost` 를 빼서 잠급니다.

## 4. 첫 실행

1. `bindery.html` 을 열고 Re:Call 과 같은 회사 코드·아이디로 들어갑니다
2. **설정 → 공급자 정보** 에 사업자등록번호·대표자·주소·업태·종목·계좌를 넣습니다
   (관리자만 고칠 수 있습니다. 여기 넣은 것이 명세서 왼쪽 "공급자" 칸에 그대로 찍힙니다)
3. **새로 만들기** 에서 첫 프로젝트를 넣습니다
4. **공정 → + 기록** 으로 한 줄 남겨 봅니다
5. **고객사 공유** 를 켜고 나온 주소를 다른 브라우저(시크릿 창)에서 열어 봅니다

## 로컬에서 볼 때

```bash
cd /Users/motodna/Desktop/Rebind
python3 -m http.server 8791
```

`http://localhost:8791/bindery.html` 로 엽니다.

---

## 잘 안 될 때

| 증상 | 볼 곳 |
|---|---|
| 로그인은 되는데 목록이 비어 있음 | 1번 SQL 을 실행했는지 |
| 고객사 링크가 "열 수 없는 주소" | 2번 배포에 `--no-verify-jwt` 가 빠졌는지 · `ALLOWED_ORIGIN` 에 그 주소가 있는지 |
| 사진이 안 올라감 | 1번 SQL 의 `works` 보관함이 만들어졌는지 |
| 명세서 공급자 칸이 비어 있음 | 설정 → 공급자 정보를 관리자 계정으로 저장했는지 |

[함수 로그](https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn/functions)와
브라우저 콘솔을 함께 보면 원인이 나옵니다.
