# Re:Bind — 진행 상황

제조 공정과 거래명세서 · DNA Labs

마지막 정리: 2026-08-27 (밤 · 금액 잠금)

## 링크

| | |
|---|---|
| **앱 (운영)** | https://rebind.dnalabs.kr/ |
| 옛 주소 | https://motodna.github.io/Rebind/ — 새 주소로 넘겨 줍니다 (`?t=…` 도 실려 갑니다) |
| **Re:Call** | https://recall.dnalabs.kr/ (2026-08-28 에 함께 옮겼습니다) |
| **저장소** | https://github.com/MotoDNA/Rebind |
| **Supabase 프로젝트** | https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn |
| **함수 로그** | https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn/functions |
| **SQL 편집기** | https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn/sql/new |

로그인은 **Re:Call 과 같은 회사 코드·아이디·비밀번호** 입니다.

| 회사 코드 | 아이디 | 무엇이 들어 있나 |
|---|---|---|
| **BKT** | admin | 비케이티 — 2026-08-28 에 새로 만들었습니다. **처음에는 비어 있습니다** |
| ACTIVA | admin | 액티바코리아 — 지금까지 넣어 둔 16건이 여기 있습니다 |

**회사가 다르면 서로의 자료가 안 보입니다.** 화면이 가리는 것이 아니라
데이터베이스가 회사로 가릅니다. 한쪽에서 만든 프로젝트는 다른 쪽에 안 뜹니다.

---

## 무엇을 만들었나

제본소가 만드는 것을 **공정 단위로 기록**하고, 고객사 담당자는 **링크만 열어** 지금
어디까지 갔는지 봅니다. 공정이 끝나면 그동안 쌓인 숫자가 **그대로 거래명세서**가 됩니다.

Re:Call 과 **같은 Supabase 프로젝트, 같은 계정**을 씁니다. 계정을 따로 만들지 않습니다.

```
Re:Call  network-dna.html   고객 · 명함 · 미팅 타임라인   (~/Desktop/network-dna)
Re:Bind  bindery.html       프로젝트 · 공정 · 명세서       (~/Desktop/Rebind)
              └── 같은 데이터베이스 · 같은 로그인
```

## 만든 순서 — 고객에게 설명할 때

| | 무엇을 | 왜 그 순서인가 |
|---|---|---|
| **1. 그릇** | 데이터베이스 표 3개와 접근 규칙 | 그릇 모양은 나중에 바꾸기가 가장 어렵습니다. 이미 쌓인 기록을 다 옮겨야 하니까요 |
| **2. 문지기** | 서버 함수 `share-view` | 고객사는 계정이 없습니다. 링크의 열쇠를 확인하고 그 프로젝트 하나만 꺼내 줍니다 |
| **3. 화면** | `bindery.html` | 넣는 쪽(직원)과 보는 쪽(고객사)이 한 파일에 들어 있습니다 |
| **4. 종이** | 거래명세서 | 다시 입력하지 않습니다. 쌓인 것이 그대로 종이가 됩니다 |

뒤로 갈수록 고치기 쉽습니다.

---

## 지금 상태

### 다 된 것 — 코드

- **표 3개와 접근 규칙** — `sql/0003_works.sql`
  - `projects` 프로젝트 하나 = 한 줄 (사양 · 금액 · 일정 · 공유 토큰)
  - `project_steps` 공정 기록 (공정명 · 전체에서 몇 % · 일자 · 메모 · 사진)
  - `company_settings` 명세서에 찍히는 우리 회사 정보 (회사마다 한 줄, 관리자만 고침)
  - `works` 사진 보관함 — 경로가 `{회사id}/{프로젝트id}/…` 라 폴더 이름이 곧 권한
  - 규칙은 Re:Call 의 고객 규칙을 그대로 본떴습니다 (관리자 · 담당 · 공유받은 사람)
- **서버 함수 `share-view`** — 토큰을 확인하고 그 프로젝트 하나만 돌려줍니다.
  내부 메모 · 담당자 id · 다른 프로젝트는 나가지 않습니다. 사진은 한 시간짜리 주소로만.
  1분에 60번을 넘겨 부르면 늦춥니다.
- **앱 `bindery.html`** — 파일 하나. 주소에 `?t=…` 가 붙어 있으면 고객사 화면으로,
  없으면 직원 화면으로 스스로 갈립니다.
  - 프로젝트 목록 (상태 칩 · 검색 · 급한 것이 위로)
  - 공정 타임라인, 제본소에서 흔한 순서 11개를 눌러서 채우기
  - **알아서 바뀌는 상태** — 첫 기록이 올라가면 진행 중, 100% 면 완료 + 납품일 기록
  - 제조 상세 · 금액(추가 품목 포함) · 완성본 사진 · 내부 메모
  - 현황 탭 — 납기 지난 건 · 이번 주 납기 · 최근 이레의 공정
  - 거래명세서 A4, **PDF(인쇄) · 엑셀(.xlsx) · 링크**
  - 25초마다 조용히 다시 받아 옵니다. 실시간 알림이 오면 그때 바로.
    고치는 중(시트가 열려 있을 때)에는 건드리지 않습니다.

### 화면으로 확인한 것

로컬에서 가짜 자료를 넣어 그려 보았습니다.

- 목록 · 상태 칩 · 진척 막대 — 정상
- 상세 · 공정 타임라인 · 제조 상세 — 정상
- 금액 셈 — 3,000부 × 1,850원 + 추가 150,000원 = 공급가 5,700,000 / 부가세 570,000 / 합계 6,270,000 ✓
- 거래명세서 A4 — 공급자/공급받는자, 품목표, 사양, 공정 내역(총 소요일 16일), 계좌·안내문 ✓
- 고객사 공개 화면 — 진행도 · 공정 · 사양 · 금액 ✓

### 설치 — 끝났습니다

| | | |
|---|---|---|
| 표와 접근 규칙 | `sql/0003_works.sql` | 표 3개 · 규칙 11개 · `works` 보관함(규칙 4개) 확인 |
| 서버 함수 | `share-view` | 배포 완료. 로그인 없이 불러지고 정해 둔 주소에서만 |
| 앱 올리기 | GitHub Pages | https://rebind.dnalabs.kr/ |

**실제 링크로 왕복 확인했습니다.** 시험용 프로젝트를 하나 넣고 공유 링크를
로그인하지 않은 상태에서 열어, 진행도 45% · 공정 세 줄 · 사양 · 금액이 나오는 것과
**내부 메모가 나가지 않는 것**을 확인한 뒤 지웠습니다. 지금 프로젝트는 0건입니다.
껐던 토큰으로 다시 부르면 404 가 돌아옵니다.

### 권한 · 서류 · 견적서 (2026-08-27 저녁)

**관리자와 직원이 보는 것이 달라졌습니다.**

| | 관리자 | 직원 |
|---|---|---|
| 프로젝트 · 공정 기록 · 완성본 사진 | 봅니다 | 봅니다 |
| 금액 · 내부 메모 | 봅니다 | **안 보입니다** |
| 고객사 공유 · 거래명세서 · 청구서 · 견적서 | 봅니다 | **안 보입니다** |
| 아래 탭 | 5개 (견적서 포함) | 4개 |

> ✅ **2026-08-27 밤에 진짜로 막았습니다.** 아래 "금액을 딴 표로 뺐습니다" 를 보세요.
> 이제 개발자 도구를 열어도 직원에게는 금액이 안 갑니다.

**서류가 셋이 됐습니다.** 담는 값은 같고 종이 모양만 다릅니다.

- **거래명세서** — 납품하고 나서. 공정 내역과 완성본 사진이 함께
- **청구서** — 제본소에서 쓰던 종이 양식 그대로. 줄마다 세액, 총합계금액을 한글로
- **견적서** — 청구서와 같은 틀. "견적금액" 과 유효기간 30일 안내

**견적서 탭이 생겼습니다.**

- 입력 칸은 새 프로젝트와 같습니다. 폼을 두 벌 두면 한쪽만 고치는 실수가 나서 하나를 돌려 씁니다
- 저장한 견적서를 누르면 종이가 뜨고, 거기서 **고치기** 와 **프로젝트로 옮기기**
- 새 프로젝트 화면 맨 위의 **견적서에서 불러오기** 로도 끌어옵니다

**고객사 칸을 누르면 최근 다섯 곳이 뜹니다.** 고르면 담당자·연락처는 물론
**지난번 사양(규격·색상·표지/내지 용지·제본·후가공·단가)까지** 그대로 채웁니다.
수량과 발주번호만 비워 둡니다 — 그건 매번 다르니까요.

**용지를 표지와 내지로 나눴습니다.** 예전에 한 칸에 몰아 적어 둔 것은 그대로 보입니다.

**제조 상세에 칸을 직접 더할 수 있습니다.** 칸 이름부터 적습니다. 링 색상, 귀도리,
포장 방법처럼 품목마다 다른 것들을 위해서입니다.

### 월말에 한 거래처로 몰아 청구하기

프로젝트 목록에서 **묶어서 청구** 를 누르면 고르기가 켜집니다.
카드를 **길게 눌러도** 켜집니다.

1. 검색칸에 거래처 이름을 넣어 그 곳 것만 걸러 놓고
2. 카드를 눌러 고르면 (또는 **전체**)
3. 아래 띠에 **몇 건 · 합계 · 거래처 · 몇 월분** 이 뜨고
4. **청구서** 를 누르면 한 장에 **날짜별로** 적힙니다

품목 표에 **날짜 칸이 하나 늘어납니다.** 납품일 순으로 서고, 한 프로젝트에
박스·용달비 같은 줄이 딸려 있으면 그 줄은 날짜를 비워 그 건에 속한 것으로 보입니다.

**거래처가 섞이면 만들지 않습니다.** 청구서는 한 곳으로 나가는 종이라
섞인 곳 이름을 알려 주고 멈춥니다. 검색으로 한 곳씩 걸러 주세요.

엑셀로 내려받아도 날짜·품명·수량·단가·공급가액·세액·합계가 그대로 나옵니다.

### 정산 — 청구서 발송 · 세금계산서 · 수금

프로젝트마다 세 가지를 따로 체크합니다. **켜고 끄는 값이 아니라 날짜로 둡니다.**
"했다/안 했다" 만 남기면 "언제 청구했더라", "이거 며칠째 안 들어오지" 를 셀 수 없습니다.
켜면 오늘 날짜가 박히고, 옆 칸에서 다른 날로 고칠 수 있습니다.

| 어디에 | 무엇이 |
|---|---|
| **프로젝트 상세 → 정산** | 세 줄. 켠 것은 날짜가, 안 켠 것은 "아직" 이 뜹니다.<br>수금이 안 됐으면 **"납품하고 21일째 못 받았습니다"** 가 붙습니다 |
| **목록 카드** | `청` `세` `수` 세 글자. 끝난 것만 초록으로 |
| **목록 칩** | **미수금** 칩이 하나 늘었습니다. 누르면 못 받은 것만 |
| **현황** | 미수금 합계와 건별 경과 일수. 오래된 것부터 위로 |
| **청구서 화면** | **"이 n건을 청구 완료로 표시"** — 묶음 청구서를 뽑고 그 자리에서 한꺼번에 |

뽑는 것만으로 자동으로 찍지는 않습니다. 미리 보기만 하고 안 보내는 일도 있으니까요.

관리자만 봅니다. 직원 화면에는 정산 칸도, 청·세·수 표시도 없습니다.

### BKT 미팅 반영 (2026-08-27 밤)

| 요청 | 한 것 |
|---|---|
| 거래처 즐겨찾기 | 고객사 자동완성에서 별을 답니다. 별 단 곳이 맨 위로.<br>**사람 단위**라 내가 단 것은 나만 보이고, 기기를 바꿔도 따라옵니다 |
| 표지/내지 용지 구분 | **이미 되어 있었습니다** (8/27 낮 반영분) |
| 제조 상세 직접 항목 추가 | **이미 되어 있었습니다** — "그 밖의 항목" 에서 칸 이름부터 적습니다 |
| 검색하면 필터 숫자도 바뀌게 | 칩 숫자가 검색 결과를 셉니다. 거래처를 찾으면 그 곳 것만 몇 건인지 바로 보입니다 |
| 이미 청구한 건 재청구 확인 | 한 건이면 "이미 청구한 건입니다 (8.8 발송). 계속하시겠습니까?"<br>여러 건이면 "이미 청구한 프로젝트가 2개 있습니다. 계속 하시겠습니까?" |
| 여러 건 정산 체크 | 아래 띠에 **정산** 버튼. 청구서·세금계산서·수금을 골라 한 번에 표시하거나 지웁니다 |
| PC 화면이 불편함 | 아래 참고 |

### PC 화면

폰에 맞춘 한 줄짜리 화면(540px)을 그대로 키우니 가운데만 길쭉하고 양옆이 비었습니다.
**화면이 1000px 보다 넓어지면** 이렇게 바뀝니다.

- 폭이 1080px 로 넓어집니다
- 목록·현황·상세·입력 폼이 **두 줄로** 섭니다. 새 프로젝트는 스크롤 없이 한 화면에 다 들어옵니다
- 시트가 아래에서 올라오지 않고 **가운데 대화상자**로 뜹니다
- 거래명세서 A4 도안(794px)이 **줄어들지 않고 그대로** 들어갑니다
- 마우스를 올리면 짚어 줍니다

폰에서는 지금까지와 똑같습니다. 같은 파일 하나입니다.

### 작업의뢰서를 찍으면 프로젝트가 만들어집니다 (2026-08-27 밤)

**새 프로젝트 → 작업의뢰서 촬영.** 사진을 찍으면 서버가 표를 읽어 칸을 채웁니다.

받은 샘플 7장으로 맞춰 두었습니다. 양식이 여섯 가지나 되는데(솜씨컴퍼니·위드프린팅·
ezette·제일미디어 등) 칸 이름이 제각각이라 **위치가 아니라 뜻으로 찾게** 했습니다.

| 잘 읽히는 것 | 사람이 봐야 하는 것 |
|---|---|
| 제품명 · 수량 · 규격 · 면수 · 도수 | **고객사** — "발주처" 와 "입고처 · 제책사" 를 헷갈립니다 |
| 표지/내지 용지 · 제본 방식 · 후가공 | **납기일** — 발주일과 입고예정일이 나란히 있으면 헷갈립니다 |
| 담당자 · 연락처 · 발주번호 | **단가** — 의뢰서에 아예 없습니다. 직접 넣으셔야 합니다 |

확실하지 않은 칸은 **노란색으로 표시**되고 위에 무엇을 봐야 하는지 적힙니다.
**바로 저장되지 않습니다.** 눈으로 보고 "만들기" 를 눌러야 들어갑니다.

원본 사진은 프로젝트에 붙어 남습니다(상세 → 작업의뢰서). 나중에 "몇 부라고 했더라"
할 때 대조합니다. **완성본 사진과 따로 둡니다** — 그건 명세서와 고객사 화면에 나가니까요.

샘플로 맞추다 실제로 잡은 것 하나: **육공타공을 스프링제본으로 잘못 읽었습니다.**
구멍만 뚫는 일과 링을 끼우는 일은 다른데 둘 다 "타공" 이 들어가서입니다.
"링을 끼운다는 말이 없으면 타공이라고만 적어라" 로 고쳤습니다.

**값** — 한 장에 약 20원입니다 (2026-08-28 단가로 다시 셈했습니다. 전에 "10원 안팎"
이라고 적어 둔 것은 제가 틀렸습니다). claude-sonnet-5 를 쓰고 입력 100만 토큰에 $2,
출력 100만 토큰에 $10 입니다. 사진은 앱이 2400px 로 줄여 보내고 API 가 다시 1568px 로
줄여 약 2,300 토큰, 지시문이 1,500~2,000 토큰, 답이 400~600 토큰입니다.

⚠ **하루 한도 200장은 회사당이 아니라 사람당입니다.** 직원이 다섯이면 하루 1,000장,
2만 원까지 나갈 수 있습니다. 그리고 이 Supabase 프로젝트에 로그인할 수 있는 계정이면
Re:Call 쪽 계정도 부를 수 있습니다. 한도는 `ORDER_DAILY_LIMIT` 로 낮출 수 있습니다.
열쇠 자체는 서버 환경변수에만 있고 브라우저로 내려가지 않습니다.

### 자재 입고 (2026-08-27 밤)

고객사가 인쇄를 마친 종이를 BKT 로 보냅니다. 표지·내지·후표지가 따로 오고
다 들어와야 공정을 시작할 수 있습니다. 그래서 **입고 카드를 공정 위에** 놓았습니다.

- 프로젝트 상세 → **입고**. 자재마다 줄 하나, 들어오면 켭니다 (날짜가 박히고 고칠 수 있습니다)
- 처음이면 **"사양에서 가져오기"** — 적어 둔 표지/내지 용지로 줄을 만듭니다
- 후표지 용지 · 면지 · 띠지 · 간지 · 스프링(링) · 삼각대 · 박스는 눌러서 더합니다
- 아직 안 들어온 게 있으면 **공정 카드에 "아직 안 들어온 자재가 2개 있습니다"**
- 목록 카드에 `입고 1/3`, 현황에 **입고 대기** 묶음

표를 새로 만들지 않고 칸 하나(jsonb)에 담았습니다. 품목이 프로젝트마다 다르고
항상 그 프로젝트와 함께만 읽기 때문입니다.

### 금액을 딴 표로 뺐습니다 (2026-08-27 밤)

지금까지 금액·정산일·내부메모는 `projects` 표 안에 있었습니다. 화면에서는 직원에게
안 보였지만 **데이터베이스는 회사 사람 모두에게 그 칸을 내려 주고 있었습니다.**
개발자 도구를 열 줄 아는 직원이라면 보고, 고칠 수도 있었습니다.

`project_money` 라는 표를 새로 만들어 옮겼습니다 (`sql/0009_money.sql`).
그 표는 **관리자만** 읽고 씁니다. 규칙이 데이터베이스에 걸려 있어 화면을 우회해도 소용없습니다.

| 옮긴 것 | 단가 · 부가세율 · 추가 품목 · 청구일 · 세금계산서일 · 수금일 · 내부메모 |
|---|---|
| 남은 것 | 프로젝트 이름 · 고객사 · 사양 · 공정 · 사진 · 입고 (모두가 봐야 하는 것들) |

**견적서도 줄 자체가 안 보입니다.** 견적서는 금액이 본체라, 금액을 뺀 견적서를
직원에게 보여 줄 이유가 없습니다.

**왜 "칸 단위 권한" 이 아니라 표를 나눴나** — 데이터베이스는 칸마다 권한을 줄 수
있지만 그 단위가 '역할' 입니다. 여기서는 관리자든 직원이든 로그인하면 모두
같은 역할이라 칸으로는 가를 수가 없습니다. 관리자냐 아니냐는 줄 단위 규칙으로만
물을 수 있고, 줄 단위 규칙은 표에만 걸립니다.

**확인했습니다.** 직원 계정으로 흉내 내어 데이터베이스에 직접 물어보니,
볼 수 있는 프로젝트는 있는데 금액 줄은 0개였습니다. 관리자는 16개 다 보입니다.

**옛 금액 칸도 지웠습니다** (2026-08-28 · `sql/0011_money_drop_old.sql`).
`projects` 표에서 `unit_price · vat_rate · extra_items · billed_on · taxed_on ·
paid_on · memo` 일곱 개가 사라졌습니다. 16건 전부 값이 일치하는 것을 확인하고
지웠고, 되돌릴 수 있게 `projects_money_backup_20260828` 표에 떠 두었습니다
(그 표는 관리자도 못 읽습니다. 서버만 읽습니다).

지운 뒤에 다시 확인했습니다 — 직원은 프로젝트를 읽을 수 있고(`select *` 가
그대로 됩니다) 금액 줄은 0개입니다. 고객사 공개 링크도 단가·표지·내지 모두
정상입니다.

덤으로 하나 고쳤습니다 — **고객사 공개 화면에 표지·내지 용지가 그동안 안 나가고
있었습니다.** 8월 27일 낮에 용지를 둘로 나누면서 서버 함수에 반영이 안 됐던 것입니다.

### 입고에 표지·내지가 기본으로 놓입니다 (2026-08-27 밤)

전에는 **사양에서 가져오기** 를 눌러야 줄이 생겼습니다. 안 누르면 입고 칸이 계속
비어 있었습니다. 이제 프로젝트를 만들면 **표지 용지 · 내지 용지 두 줄이 처음부터**
놓입니다. 하나만 오는 건이면 ✕ 로 빼면 됩니다.

**평량은 그 줄에 바로 적습니다.** 용지 이름 아래 흐린 글씨를 누르면 칠 수 있습니다
— "아트지 250g" 처럼 종이 이름과 평량을 함께 적습니다. 여기서 고치면
**제조 상세의 표지/내지 용지 칸도 같이 바뀝니다.** 한쪽만 고쳐 두면 나중에 어느
쪽이 맞는지 헷갈리기 때문입니다.

따로 들어오는 것(후표지 용지 · 면지 · 띠지 · 간지 · 스프링 · 삼각대 · 박스)은
아래 버튼으로 더합니다. 있던 프로젝트 16건에도 두 줄을 채워 두었습니다
(`sql/0010_arrivals_seed.sql`).

### 작업의뢰서 인식 — 샘플을 다시 읽고 지시문을 조였습니다 (2026-08-27 밤)

갖고 계신 샘플 7장 중 넷(제일미디어 · 솜씨컴퍼니 · 위드프린팅 · ezette)을
다시 들여다보고, 지금 지시문이 틀리게 읽을 자리를 다섯 군데 찾았습니다.

| 무엇이 문제였나 | 어떻게 고쳤나 |
|---|---|
| **고객사** — 위드프린팅 양식은 "거래처: 투디자인" 이라고 적혀 있습니다. 하지만 우리에게 일을 준 건 위드프린팅입니다 | **의뢰서를 발행한 회사**(맨 위 로고 · 맨 아래 상호)가 고객사입니다. "거래처" 칸은 그 회사의 고객이라 우리와 다를 수 있습니다. 둘이 다르면 노란 표시 |
| **담당자** — 솜씨컴퍼니 양식에는 담당자가 둘입니다. 발주처 최태선, 물류 임현자 | 발주처 쪽 담당자만 봅니다 |
| **제본** — 위드프린팅 건은 품명이 "링 단어장" 인데 우리가 할 일은 "재단+타공" 뿐입니다 | **제품 이름 속 "링" 은 근거가 아닙니다.** 작업 지시 칸만 봅니다. 제본 업체가 여러 줄이면 "비케이티" 라고 적힌 줄만 |
| **납기일** — 읽은 넷 중 셋에 납품일이 **아예 없습니다** | 없으면 비우고 노란 표시. 발주일을 대신 넣지 않습니다 |
| **그 밖의 항목** — "날개 X · 띠지 X · 귀따기 없음" 을 항목으로 만들어 버립니다 | X · 없음 은 없다는 뜻이니 넣지 않습니다 |
| **용지** — "200g 스노우 5*7" 의 5*7 은 절수입니다 | 종이 이름과 평량만 적습니다 |

**아직 실기로 돌려 보지는 못했습니다.** 함수를 부르려면 로그인이 필요한데 제가
로그인을 못 합니다. 샘플을 폰으로 한 장 찍어 보시고 어느 칸이 틀리는지 알려 주시면
그 자리를 다시 조입니다.

### 안드로이드 홈 화면 아이콘

**안드로이드용 앱(Capacitor)은 Re:Bind 에 없습니다.** `~/Desktop/network-dna/app` 에
있는 것은 Re:Call 것이라 건드리지 않았습니다. Re:Bind 는 브라우저에서 열고
홈 화면에 얹어 쓰는 방식입니다.

문제는 **안드로이드 크롬이 아이폰용 아이콘(`apple-touch-icon`)을 보지 않는다**는
것이었습니다. 크롬은 `manifest` 라는 파일만 봅니다. 그게 없어서 홈 화면에 얹으면
DNA Labs 마크가 아니라 크롬 기본 조각이 붙었습니다.

`make-logo.py` 가 이제 아이콘 세 장과 manifest 를 함께 만듭니다.

```bash
python3 make-logo.py     # logo.png · icon-192 · icon-512 · icon-maskable-512 · manifest
```

세 장인 이유: 안드로이드는 기기마다 아이콘을 제 모양대로(원·사각·물방울)
잘라 냅니다. 원으로 그린 마크를 주면 한 번 더 잘려 글자가 잘립니다.
그래서 네모를 꽉 채운 것(maskable)을 한 장 더 둡니다.

홈 화면에 얹으면 이름은 **Re:Bind**, 주소창 없이 앱처럼 열립니다.

### 단위 고르기 · 사진은 촬영과 앨범 둘 다

**단위**를 골라 넣습니다 — 부 · 권 · 장 · 세트 · 기타 · **직접입력**.
직접입력을 고르면 아래에 칸이 열립니다. 예전에 "매" 처럼 적어 둔 것이 있으면
열었을 때 자동으로 직접입력에 그 값이 들어가 있습니다. 새 프로젝트와 제조 상세 양쪽 다.

**사진**은 누르면 **촬영하기 / 앨범에서 고르기**를 먼저 묻습니다.
폰에 따라 사진 칸을 그냥 누르면 곧장 카메라만 열리는 것이 있어 두 길을 따로 두었습니다.
작업의뢰서와 완성본 사진 둘 다 그렇습니다.

---

## 이어서 할 때

새 대화에서 이렇게 시작하시면 됩니다.

```
/rebind 이어서 작업할게
```

`.claude/skills/rebind/` 에 스킬을 넣어 두었습니다. 이 파일(진행상황.md)과 함께
어디에 무엇이 있는지, 무엇을 지켜야 하는지가 들어 있습니다.

### 지금 상태 — 다 올라가 있습니다

운영 주소를 실제로 열어 콘솔까지 확인했습니다. 오류 없습니다.

| | |
|---|---|
| 앱 | 프로젝트 · 공정 · 자재 입고 · 정산 · 견적서 · 명세서 · 청구서 |
| 서버 함수 | `share-view`(고객사 링크) · `read-order`(작업의뢰서 인식) 배포됨 |
| 표 | 0003~0008 모두 적용됨 |
| 시연 자료 | 13건 (월드김영사 3 · 이제트 7 · 그 밖 3) |

### 스캔은 확실한 것만 넣습니다 (2026-08-28)

**애매하면 넣지 않고 비웁니다.** 반쯤 맞는 값이 들어가 있으면 사람이 맞는 줄 알고
그냥 넘기지만, 비어 있으면 반드시 보게 됩니다.

비우는 경우는 셋입니다 — 글씨가 흐리거나 잘려서 못 읽을 때, 비슷한 칸이 둘인데
어느 쪽인지 모를 때(발주처와 거래처 · 발주일과 납품일), 적혀 있긴 한데 그 뜻이
우리가 찾는 항목이 맞는지 자신이 없을 때.

앱도 함께 고쳤습니다. **지금까지는 값이 비면 노란 표시까지 사라졌습니다** —
비어 있는 칸에는 표시할 값이 없다고 건너뛰었기 때문입니다. 이제는 반대로,
비었을 때야말로 노랗게 칠합니다. 안내문도 "확실하지 않습니다" 에서
"확실하지 않아 비워 두었습니다. 노란 칸을 직접 채워 주세요" 로 바꿨습니다.

### 주소를 dnalabs 로 (2026-08-28)

**`dnalabs.kr` 을 가비아에서 샀습니다.** 주소는 **`https://rebind.dnalabs.kr/`** 로
정했습니다. Re:Call 은 나중에 `recall.dnalabs.kr` 로 나란히 둘 수 있고,
`dnalabs.kr` 뿌리는 회사 소개 자리로 비워 둡니다.

| 한 것 | |
|---|---|
| `ALLOWED_ORIGIN` | `rebind.dnalabs.kr` 과 `motodna.github.io` 둘 다 허용하도록 미리 바꿔 배포했습니다. 도메인이 살아나는 순간 바로 되게 |

| 가비아 DNS | CNAME · 호스트 `rebind` · 값 `motodna.github.io.` |
| 저장소 | `CNAME` 파일 한 줄 |
| GitHub | 도메인 지정 · HTTPS 강제 켜기 (인증서는 바로 나왔습니다) |

**순서를 지켰습니다.** DNS 가 퍼진 것을 확인한 뒤에 `CNAME` 파일을 올렸습니다.
먼저 올렸으면 GitHub 이 옛 주소를 아직 안 열리는 새 주소로 넘겨 앱이 잠깐 죽습니다.

**이미 보낸 공유 링크는 살아 있습니다.** 걱정하던 부분인데, 확인해 보니
GitHub 이 `?t=…` 를 그대로 실어 넘깁니다.

```
https://motodna.github.io/Rebind/?t=3d81…
  → 301 → https://rebind.dnalabs.kr/?t=3d81…
```

새 주소로 앱과 고객사 화면을 실제로 열어 콘솔까지 확인했습니다. 오류 없습니다.

**Re:Call 도 같은 날 옮겼습니다** — `https://recall.dnalabs.kr/`.
공유 링크를 쓰지 않는 앱이라 밖으로 나가 있는 주소가 없었고, 안드로이드 앱은
화면을 앱 안에 담아 두는 방식이라 웹 주소와 상관없습니다. 옛 주소는 넘어갑니다.

> ⚠ **`ALLOWED_ORIGIN` 은 두 앱이 함께 쓰는 값 하나입니다.** 같은 Supabase
> 프로젝트라서요. 지금은 `rebind.dnalabs.kr,recall.dnalabs.kr` 둘뿐이고
> `motodna.github.io` 는 뺐습니다 (옛 주소는 어차피 새 주소로 넘어가므로
> 그 주소에서 함수를 부를 일이 없습니다).
>
> **이 값을 바꾸면 네 함수를 모두 다시 배포해야 반영됩니다** —
> `share-view` · `read-order` (Re:Bind) · `read-card` · `admin-user` (Re:Call).
> 네 함수 × 세 주소로 실제로 눌러 확인했습니다. 새 주소 둘은 통과, 옛 주소는 막힘.

> 📌 **Re:Call 저장소에 안 올린 것이 있습니다.** 8월 27일에 Re:Bind 를 떼어내며
> `bindery.html` · `share-view` · `0002_works.sql` 을 지우고 진행상황.md 에 적어 둔
> 변경이 커밋되지 않은 채로 있습니다. 오늘 넣은 `CNAME` 커밋에는 섞지 않았습니다 —
> 남의 손이 하던 일을 대신 마무리해 버리면 나중에 무엇이 언제 된 것인지 흐려집니다.
> 그대로 두었으니 확인하고 올리시면 됩니다.

`motodna` 는 GitHub 계정 이름입니다. 그 자리를 `dnalabs` 로 바꾸려면 계정이나
조직 이름이 `dnalabs` 여야 하는데 **이미 다른 사람이 쓰고 있습니다**
(`dnalabs` 2012년, `dna-labs` 2021년에 만들어진 조직).

그래서 **도메인을 사서 연결하기로** 했습니다. `rebind.dnalabs.kr` 처럼
원하는 대로 지을 수 있고, 나중에 GitHub 을 떠나도 주소가 안 바뀝니다.
Re:Call 도 `recall.dnalabs.kr` 로 나란히 둘 수 있습니다.

**도메인 사기는 직접 하셔야 합니다** (결제라 제가 못 합니다). 사신 뒤에 알려 주시면
아래를 제가 합니다.

| 할 일 | 무엇 |
|---|---|
| DNS | GitHub Pages 로 향하게 A 레코드 4줄 또는 CNAME 한 줄 |
| 저장소 | `CNAME` 파일 한 줄 + Pages 설정에서 도메인 지정 · HTTPS 켜기 |
| 넘겨주기 | 지금 주소(`motodna.github.io/Rebind/`)에 새 주소로 보내는 페이지를 남깁니다. **이미 고객사에 보낸 공유 링크가 안 죽게** `?t=…` 를 그대로 실어 보냅니다 |
| 함수 | `ALLOWED_ORIGIN` 에 새 주소를 넣고 `share-view` 재배포 |
| 문서 | 진행상황.md · README · 스킬의 주소 갱신 |

### 로그인이 안 되던 진짜 이유 (2026-08-28)

BKT 계정을 만들고도 로그인이 안 됐습니다. **계정 문제가 아니었습니다.**

로그인 자체는 됩니다. 그다음 `loadMe()` 가 프로필을 읽어 오는데 그게 실패해서
곧바로 로그아웃되고 있었습니다 — "계정 정보를 불러오지 못했습니다" 가 뜨는 자리입니다.

프로필을 읽으면서 회사 이름을 함께 붙이는데, 서버가 **"어느 길로 이어진 회사냐"** 를
못 골랐습니다. `profiles.company_id` 로 바로 이어진 길 말고도, 8월 27일에 넣은
**거래처 즐겨찾기**(`client_favorites`)가 사람과 회사를 함께 물고 있어서 그것도
후보가 되기 때문입니다. 후보가 둘이면 서버는 300 을 돌려주고 아무것도 주지 않습니다.

어느 연결인지 이름으로 못 박아 고쳤습니다.

```
companies(code,name)                          ← 후보가 둘이라 실패
companies!profiles_company_id_fkey(code,name) ← 이 길이라고 못 박음
```

**언제부터 이랬는지는 잘라 말하기 어렵습니다.** 즐겨찾기를 넣은 8월 27일부터일 수도,
오늘 표를 여러 번 고치면서 서버가 스키마를 다시 읽은 뒤부터일 수도 있습니다.
서버는 표 구조를 외워 두었다가 바뀔 때 다시 읽는데, 그때 이 모호함이 드러납니다.

확실한 것 하나는 적어 둡니다 — **제가 지금껏 "운영 주소를 열어 콘솔까지 봤다" 고
한 것은 전부 로그인 화면이 뜨는 것까지였습니다.** 실제로 들어가 보지는 않았습니다.
표를 고친 날에는 로그인 경로까지 확인해야 합니다.

> ⚠ **Re:Call 도 같은 줄을 씁니다.** `network-dna.html` 에 똑같은 쿼리가 있어
> 지금 Re:Call 도 로그인이 안 될 것입니다. 고치는 것은 그 한 줄뿐입니다.
> 그 저장소는 따로 하신다고 해서 손대지 않았습니다.
> **안드로이드 앱은 화면을 앱 안에 담아 두므로, 고친 뒤 다시 담아야 반영됩니다.**

### BKT 계정을 만들었습니다 (2026-08-28)

작업의뢰서 스캔을 시험하려는데 로그인이 안 된다고 하셔서 봤더니,
**`BKT` 회사가 아예 없었습니다.** 지금까지 있던 것은 `ACTIVA` 하나뿐입니다.

앱이 계정을 만드는 정식 경로(`admin-user` 의 `bootstrap`)와 같은 순서로 만들었습니다 —
회사 → 로그인 계정 → 프로필 → 감사 기록. 중간에 실패하면 앞에서 만든 것을 도로 지웁니다.

만들고 나서 **앱이 하는 것과 똑같은 호출로 실제 로그인까지 확인했습니다.**
프로필과 회사가 제대로 읽히고, 보이는 프로젝트는 0건입니다 (아래 참고).

**회사 코드 규칙을 3자부터로 풀었습니다** (`sql/0012_company_code_3.sql`).
4~12자만 받고 있었는데 `BKT` 는 세 글자였습니다. 처음 정할 때 근거 없이 잡은 값이고,
세 글자 회사 이름은 흔합니다. 느슨하게 푸는 것이라 이미 있는 줄은 어긋나지 않습니다.

> ⚠ 앱 쪽에도 같은 규칙이 하나 더 있습니다 —
> `network-dna/supabase/functions/admin-user/index.ts` 의 `CODE_RE` 가 아직 4자 이상입니다.
> **회사를 새로 만들 때만** 쓰이므로 로그인에는 영향이 없습니다. 앞으로 세 글자 회사를
> 앱 화면에서 만들려면 그것도 같이 풀어야 합니다. Re:Call 쪽이라 손대지 않았습니다.

**BKT 로 들어가면 처음에는 비어 있습니다.** 지금까지 넣어 둔 16건(시연 13 + 실제 3)은
`ACTIVA` 것이라 안 보입니다. 스캔 시험에는 오히려 깨끗해서 좋지만, 앞으로 BKT 를
진짜로 쓰실 거면 자료를 어떻게 할지 정해야 합니다.

| 길 | |
|---|---|
| 그대로 둔다 | BKT 에서 새로 쌓습니다. ACTIVA 는 시연용으로 남겨 둡니다 |
| 옮긴다 | 16건을 BKT 로 옮깁니다. **사진이 끊깁니다** — 보관함 경로가 `{회사id}/…` 라 폴더 이름이 곧 권한이어서, 회사가 바뀌면 옛 사진을 못 읽습니다. 사진까지 옮기는 일을 따로 해야 합니다 |
| ACTIVA 이름만 바꾼다 | 가장 간단해 보이지만 **Re:Call 이 같은 회사 표를 씁니다.** 코드를 바꾸면 Re:Call 쓰는 분들의 로그인 코드도 같이 바뀝니다 |

> 비밀번호는 일곱 자입니다. 앱 화면에서 계정을 만들 때는 **열 자 이상**을 요구합니다
> (`checkPw`). 이번에는 정식 경로가 아니라 서버에서 바로 만들어 그 검사를 지났습니다.
> 시험용이면 그대로 쓰셔도 되고, BKT 를 실제로 쓰실 거면 설정에서 바꾸시는 편이 낫습니다.

### BKT 에 예시 다섯 건 (2026-08-28)

`sql/0013_demo_bkt.sql`. 갖고 계신 작업의뢰서 샘플(제일미디어 · 솜씨컴퍼니 ·
위드프린팅 · 이제트)을 본떠 만들었습니다. **다섯 건의 상태를 일부러 다 다르게**
두어서, 목록 칩·현황·명세서·청구서·정산이 한 번에 다 보입니다.

| 프로젝트 | 고객사 | 상태 |
|---|---|---|
| 2027 숭실사이버대학교 탁상캘린더 | 제일미디어 | 완료 · **미수금** (청구·세금계산서만) |
| 어른의 스도쿠 — 철학의 위로 | 솜씨컴퍼니 | 진행 65% · 납기 코앞 · **공유 링크 켜 둠** |
| A7 메모리 링 단어장 | 위드프린팅 | 진행 30% · **납기 지남** |
| 메트라이프 40절 스프링다이어리 | 이제트 | 대기 · **입고 1/4** |
| 세바로지스틱스 탁상달력 표지 | 이제트 | 완료 · 수금까지 끝남 |

**공급자 정보도 채웠습니다.** 명세서 왼쪽 칸이 비어 있으면 종이가 허전해서,
솜씨컴퍼니 의뢰서에 적혀 있던 값(대표자 신종령 · 주소 · 연락처)을 넣었습니다.
**사업자등록번호와 계좌는 비워 뒀습니다** — 제가 알 수 없는 값이라 지어내지
않았습니다. 앱 → 설정 → 공급자 정보에서 채우시면 됩니다.

지울 때는 한 줄입니다.

```bash
supabase db query --linked "delete from projects where id::text like 'c3000000-%'"
```

### 서류를 카톡으로 보내기 (2026-08-28)

명세서·청구서·견적서 화면 위에 **보내기** 가 생겼습니다. 누르면 이미지와 PDF 중
고르고, 폰의 공유창이 뜹니다. 거기서 카카오톡을 고르시면 됩니다.

**카카오톡을 콕 집어 보낼 방법은 웹에 없습니다.** 카카오 SDK 도 링크와 카드만
보내지 파일은 못 붙입니다. 그래서 공유창을 열어 주는 것까지가 앱이 할 수 있는
전부이고, 앱 한 번 · 카톡 한 번 고르시면 됩니다. 시트에 그렇게 적어 두었습니다.

| | |
|---|---|
| **이미지** | 카톡 대화창에서 **바로 보입니다.** 안 받아도 됩니다. 280KB 쯤 |
| **PDF** | 보관용·첨부용. 받는 분이 한 번 눌러 받아야 보입니다. 270KB 쯤 |
| PC 에서는 | 데스크톱 브라우저는 파일 공유를 대부분 못 합니다. **내려받기로 떨어집니다** |

**어떻게 만드나** — 지금까지 PDF 는 브라우저 인쇄뿐이었는데, 그건 사용자가 직접
"PDF로 저장"을 누르는 것이라 **파일이 코드 손에 잡히지 않습니다.** 그래서 화면에
그려 둔 A4 도안을 사진 찍듯 그대로 떠서 파일로 만듭니다. 글자를 다시 그리지 않는
이유는 한글 글꼴을 PDF 안에 넣으면 파일이 몇 MB가 되고 줄바꿈이 깨지기
때문입니다. 대신 **글자 선택·검색은 안 됩니다.** 라이브러리는 누를 때만 받습니다.

만들면서 걸린 것 둘을 적어 둡니다.

- **시트가 명세서 화면 뒤에 깔렸습니다.** 지금까지 바텀시트는 상세 화면 위에서만
  열려서 z-index 71 로 충분했는데, 명세서는 96 이었습니다. 시트를 98 로 올렸습니다
- **PDF 가 두 장으로 나왔습니다.** 명세서가 A4 보다 8% 길어서 손톱만 한 둘째 장이
  딸려 나왔습니다. 15% 까지는 살짝 줄여 한 장에 담습니다. 그보다 긴 것(줄이 많은
  묶음 청구서)은 억지로 줄이면 안 읽혀서 그냥 여러 장으로 둡니다

### 안드로이드 뒤로가기 (2026-08-28)

**뒤로가기를 누르면 앱을 통째로 떠나 로그인 화면으로 돌아갔습니다.** 꾹 눌러
고르기를 켜 놓고 뒤로가기를 누르면 하던 일이 다 날아갔습니다.

이제 **열려 있는 것부터 하나씩** 닫습니다 — 시트 → 명세서 → 상세 → 고르기.
다 닫히고 나서 한 번 더 누르면 그때 앱을 떠납니다.

아무것도 안 열려 있으면 아무 장치도 걸지 않습니다. 목록 화면에서 뒤로가기를
눌렀는데 아무 일도 안 일어나면 그게 더 답답하기 때문입니다.

### 설정에 팀원 · 무엇을 보일지 고르기 (2026-08-28)

**직원 계정 추가가 Re:Bind 에 없었습니다.** Re:Call 설정에만 있어서 계정을
만들려면 다른 앱을 열어야 했습니다. 두 앱이 계정을 함께 쓰니 되기는 됐지만,
제본소 사람들이 쓰는 건 이쪽입니다. **설정 → 팀원** 으로 넣었습니다.

계정을 만들고 비밀번호를 바꾸는 일은 브라우저가 하지 않습니다. Re:Call 이
쓰던 `admin-user` 서버 함수를 그대로 부릅니다 — 그 함수만 열쇠를 갖고 있습니다.
새로 만들 것이 없었습니다. 비밀번호 규칙(10자 이상 등)도 서버와 같게 맞췄습니다.

**자기 계정은 목록에서 못 누릅니다.** 실수로 스스로를 잠그면 들어올 길이
없어집니다. 서버도 같은 것을 막고 있습니다.

#### 무엇을 직원에게 보일지 고릅니다

**설정 → 직원에게 보일 것** 에서 셋을 따로 켜고 끕니다.
기본은 지금까지와 같은 **다 잠금** 이라 아무것도 달라지지 않았습니다.

| | 켜면 |
|---|---|
| **금액** | 단가·추가 품목·합계. **거래명세서와 청구서도 함께** 열립니다 |
| **정산** | 청구서 발송·세금계산서·수금. 미수금 목록도 함께 |
| **내부 메모** | 고객사에게는 어느 쪽이든 나가지 않습니다 |

**열어 주어도 고치는 것은 관리자만입니다.** 직원은 보기만 합니다 — 원본 표가
관리자 전용이라 직원이 눌러 봐야 저장되지 않습니다. 그래서 정산 스위치와
금액 고치기 버튼은 직원 화면에서 잠급니다. 고객사 공유와 견적서는 늘 관리자 몫입니다.

**어떻게 막나** — 줄 단위 규칙으로는 칸을 가릴 수 없습니다. 한 줄을 보여 주면
그 줄의 모든 칸이 나갑니다. 그래서 **칸을 가려 주는 창**(`project_money_v`)을
두었습니다. 앱은 창을 읽고 쓰기는 원본에 합니다. 잠근 칸은 서버가 0 과 빈칸으로
바꿔 내려 줍니다 — 개발자 도구로 직접 불러도 볼 것이 없습니다.

창을 만들다 하나 잡았습니다. **처음에는 회사만 걸렀는데**, 그러면 직원이 맡지도
않은 프로젝트의 금액까지 나갑니다. 프로젝트 권한(담당·공유받음)도 함께 걸었습니다.

금액을 잠근 채 정산만 열면 **미수금 합계를 적지 않습니다.** 가려진 값으로 더하면
0원이 되는데 그건 거짓말입니다. 건수만 적습니다.

관리자·직원 × 세 스위치 조합을 데이터베이스와 화면 양쪽에서 확인했습니다.

### 고객사 공유는 언제든 끌 수 있습니다

물어보셔서 실제로 눌러 확인했습니다. **프로젝트 상세 → 고객사 공유**
에서 스위치를 끄면 그 자리에서 막힙니다.

```
켜져 있을 때 → 열림
껐을 때      → 없는 주소입니다 (404)
다시 켰을 때  → 열림
```

**주소 새로 만들기** 도 있습니다. 링크가 새어 나갔을 때 쓰면 **앞의 주소는
바로 죽고** 새 주소가 생깁니다. 고객사에 다시 보내 주시면 됩니다.

껐다 켜면 **같은 주소가 다시 살아납니다.** 아주 막으려면 끄지 말고
주소를 새로 만드세요.

### 직원에게 프로젝트가 하나도 안 보이던 것 (2026-08-28)

직원 계정을 만들어 들어가 보니 **목록이 텅 비어 있었습니다.** 버그가 아니라
설계가 안 이어진 자리였습니다.

접근 규칙을 Re:Call 의 고객 규칙에서 그대로 본떠 왔습니다. 거기서는
"담당이거나 공유받았거나" 가 맞습니다 — 영업은 내 고객과 남의 고객이 나뉘니까요.
그런데 **Re:Bind 에는 담당자를 지정하는 기능이 아예 없습니다.** `shared_ids` 는
늘 비어 있고 `owner` 는 만든 사람 고정이라, 직원은 영영 아무것도 못 봅니다.

제본소는 공장에 올라온 일을 그날 손 비는 사람이 잡습니다. 그래서
**회사 사람이면 프로젝트를 다 봅니다** (`sql/0015_staff_sees_projects.sql`).

| 연 것 | 프로젝트 목록 · 공정 적기 · 자재 입고 켜기 |
|---|---|
| **그대로 잠긴 것** | 견적서 · 금액 · 정산 · 내부메모(설정대로) · 지우기 · 다른 회사 |

실제 계정(BKT `tester1`)으로 확인했습니다.

```
보이는 프로젝트 8 · 그중 견적서 0 · 공정 기록 22
금액 원본표를 직접 읽으면 0줄
금액 창으로는 8줄 — 단가 0 · 메모 0 · 청구일 0 (다 잠근 상태)
정산 스위치를 켜면 → 청구일 2건이 살아나고 단가는 그대로 0
ACTIVA 직원은 ACTIVA 것 16건만 — BKT 것은 안 보임
```

### 앱이 스스로 뒤로 가지 않습니다 (2026-08-28)

Re:Bind 를 쓰다 Re:Call 로 넘어간 것 같다는 말씀에 뒤로가기 처리를 다시 봤습니다.

어제 넣을 때, 앱 버튼으로 창을 닫으면 세워 둔 방어막을 `history.back()` 으로
거뒀습니다. 뒤로가기 한 번이 헛도는 것을 아끼려던 것입니다. 그런데 방어막과
실제 방문 기록이 한 칸이라도 어긋나면 **그 한 번이 앱 밖으로 나갑니다** —
같은 창에서 Re:Call 을 먼저 열었다면 그쪽으로 갑니다.

어긋나는 경로를 끝내 못 찾았습니다. 그래도 아껴 봐야 헛클릭 한 번이고
잃으면 하던 일이 통째로 날아갑니다. **그래서 그 줄을 지웠습니다.**
이제 코드 어디에도 `history.back()` 이 없습니다.

> 단정하지는 못합니다. Re:Call 을 같은 창에서 먼저 열어 두셨다면, 목록
> 화면에서 뒤로가기를 누른 것만으로 브라우저가 그쪽으로 돌아갑니다.
> 그건 브라우저가 하는 일이라 막을 수 없고, 막으면 앱에서 영영 못 나갑니다.
> 또 겪으시면 어느 화면에서 무엇을 누른 뒤였는지 알려 주세요.

### 업종별로 말을 바꿔 쓰기 (2026-08-28) — 앱까지 끝냈습니다

**왜** — 인쇄소가 이 앱을 쓰고 싶다고 하면 어떻게 할지 이야기하다 나왔습니다.
회사를 나누는 일은 이미 데이터베이스가 합니다(지금 ACTIVA·BKT 두 곳이 돕니다).
회사 하나 더 받는 건 **회사와 관리자 계정을 만드는 일뿐**이고 코드는 안 건드립니다.

**문제는 앱이 "책 만드는 말" 로만 쓰여 있었다는 것입니다.** 공정에 접지·정합·제본이
박혀 있고, 자재 입고는 표지 용지·내지 용지로 시작하고, 제조 상세에 면수와
제본 방식이 있었습니다. 전단지를 찍는 인쇄소는 안 쓰는 칸을 계속 비워 두고 씁니다.

그 목록들을 코드에서 꺼내 **회사 설정으로 옮겼습니다.**
업종은 **시작 틀**이고, 고른 뒤에는 회사가 자기 말로 고칩니다.

#### 그릇 — `sql/0017_trade.sql` (적용 끝)

`company_settings` 에 칸 둘이 붙었습니다.

| | |
|---|---|
| `trade` | 업종 이름 ('제본소' · '인쇄소'). 고르면 그 기본값이 preset 에 깔립니다 |
| `preset` | 이 회사가 실제로 쓰는 말. **비어 있으면 앱이 제본소 기본값을 씁니다** |

#### 앱 — `TRADE_PRESETS` 한 곳으로 모았습니다

코드에 흩어져 박혀 있던 넷을 `TRADE_PRESETS` 한 덩어리로 모으고,
`tradeOf()` 하나가 **회사 preset → 없으면 제본소 기본값**을 돌려줍니다.
화면은 그것만 봅니다.

| 없어진 상수 | 지금 |
|---|---|
| `STEP_SEED` | `tradeOf().steps` |
| `ARRIVAL_QUICK` · `arrivalDefaults()` | `tradeOf().arrivalAdd` · `tradeOf().arrivals` |
| `COST_QUICK` | `tradeOf().costs` |
| `SPEC_FIELDS` (상수) | `specFields()` — 이름표가 회사마다 달라 상수로 못 둡니다 |

이름표(`labels`)를 보는 자리는 제조 상세 표 · 고치기 시트 · 새로 만들기 폼 ·
거래명세서 · 견적서 · **고객사가 링크로 보는 화면**까지 전부입니다.

**칸 이름은 이름표만 바꿉니다.** `spec_paper_cover` 같은 칸 이름 자체는 그대로
두었습니다. 인쇄소가 "용지" 라고 부르는 것이 데이터베이스에는 여전히
`spec_paper_cover` 로 들어갑니다. 칸을 자유 항목으로 일반화하는 것은 훨씬 큰
일이라 이번에는 안 했습니다.

예시글(placeholder)도 함께 바꿉니다. "가공 방식" 칸 아래에 "예: 무선제본" 이
남아 있으면 이름표만 바꾼 것이 오히려 헷갈리기 때문입니다.

#### 설정 → 업종과 용어 (관리자만)

업종을 고르면 그 기본값이 **화면에만** 깔립니다. 저장은 저장 버튼을 눌러야
됩니다 — 고르자마자 저장하면 손으로 고쳐 둔 목록이 소리 없이 날아갑니다.

목록 넷은 한 줄에 하나씩 적습니다. **비워 두면 기본값으로 돌아갑니다** —
회사가 실수로 다 지웠을 때 빈 화면을 보여 주는 것보다 낫습니다.
칸 이름표 여덟 개도 여기서 고칩니다.

저장하면 이미 그려 둔 화면(목록·상세·원가 빠른 항목·새로 만들기 폼)을 다시
그립니다. 안 그러면 옛말이 남아 있습니다.

#### 고객사 화면도 같은 말을 씁니다

`share-view` 가 `preset` 을 함께 내려 줍니다. 고객사는 로그인이 없어서
회사 설정을 읽을 길이 없기 때문입니다. 앱은 그것을 `issuer.preset` 자리에
담아 두고 `tradeOf()` 가 그대로 봅니다.

#### `read-order` 에 박혀 있던 BKT — 고쳤습니다

지시문에 `"BKT · 비케이티 · bkt 는 우리 자신입니다"` 가 박혀 있었습니다.
인쇄소가 쓰면 자기 상호를 고객사로 읽어 버렸을 것입니다.
이제 함수가 부른 사람의 `company_id` 로 **상호와 회사코드를 읽어** 지시문에
끼워 넣습니다. 회사코드는 대문자로 두지만 의뢰서에는 소문자로 적히기도 해서
소문자도 함께 넣습니다. 첫 줄의 "제본소가 받은 …" 도 `trade` 를 따릅니다.

BKT 는 상호가 "비케이티", 코드가 "BKT" 라 **예전과 똑같은 말이 들어갑니다.**

#### 확인한 것

브라우저에서 인쇄소로 바꿔 보고 되돌려 봤습니다. 콘솔 오류는 없습니다.

- 인쇄소: 공정 10개 · 입고 "용지" 한 줄 · 원가 인쇄/용지/코팅… · 쪽수 · 가공 방식
- 입고 기본 줄이 사양의 용지를 그대로 물고 옵니다 (용지 ← 아트지 250g)
- 거래명세서에도 "쪽수 208쪽 · 가공 방식" 으로 나갑니다
- **설정을 비우면 지금까지와 글자 하나 다르지 않습니다** — 표지 용지 · 내지 용지 ·
  면수 208면 · 제본 방식. 두 회사 다 `preset` 이 비어 있어 **동작은 그대로입니다**

#### 아직 안 한 것

- **업종을 더 받으려면** `TRADE_PRESETS` 에 한 벌 더합니다. 코드를 고쳐야 합니다 —
  회사가 화면에서 업종 자체를 만들지는 못합니다
- **`read-order` 지시문 본문은 여전히 제본소 말**입니다 (면수 · 제본 방식 · 면지…).
  인쇄소 의뢰서를 제대로 읽히려면 지시문도 업종별로 갈라야 합니다.
  샘플이 없어 지금 손대면 넘겨짚는 일이 됩니다
- **단위(부 · 권 · 장 · 세트)** 는 아직 코드에 있습니다. 직접 적을 수 있어서
  급하지 않다고 봤습니다

#### 코드 말고 정해야 할 것

- **회사를 누가 여는가** — 지금은 `SETUP_SECRET` 을 아는 사람만 열 수 있고
  앱 안에 그 화면이 없습니다. 인쇄소를 받으려면 개설 화면이 필요합니다
- **값** — Supabase Pro 월 $25 는 프로젝트 하나 값이라 회사가 늘어도 그대로입니다.
  다만 스캔 API 는 회사별로 갈라 세지 않습니다
- **사고 났을 때** — 백업이 하나라 한 회사 자료만 되돌릴 방법이 지금은 없습니다

### 숨기기 · 여러 건 지우기 (2026-08-29)

**왜** — 접수는 했는데 고객사가 "잠깐 미뤄 달라" 고 하는 일이 있습니다.
지우면 안 되고, 그렇다고 목록 맨 위에 계속 두면 오늘 할 일이 안 보입니다.

#### 숨기기 — 보류와 다릅니다

이미 있던 **보류**(`status='hold'`)는 목록에 그대로 남습니다. 상태가 보류일 뿐입니다.
**숨기기**는 목록 · 칩 숫자 · 현황 · 미수금에서 전부 빠지고 **숨김 칩에만** 남습니다.
그래서 더보기 차림표에서 둘을 나란히 두고, 보류 밑에 "목록에는 그대로 남습니다" 라고 적었습니다.

`sql/0018_hidden.sql` — `projects.hidden_on date` (적용 끝).
체크가 아니라 날짜입니다. 정산 세 단계와 같은 판단입니다 — 체크만 해 두면
며칠째 묵고 있는지 셀 수가 없습니다. 카드와 상세에 `숨김 8.25` 로 찍힙니다.

**`deleted` 와 따로 둔 이유** — 지운 것은 서버가 아예 안 내려 줍니다(`pull` 이
`deleted=false` 로 거릅니다). 숨긴 것은 내려받아 두었다가 숨김 칩을 누르면
그 자리에서 보여 줘야 합니다. 같은 칸에 담으면 되돌릴 목록을 볼 방법이 없어집니다.

| 어디서 | 어떻게 |
|---|---|
| 상세 → 더보기 | **목록에서 숨기기** / (숨긴 건이면) **목록으로 되돌리기** |
| 목록 칩 | **숨김 N** — 숨긴 것이 있을 때만 나옵니다 |
| 숨김 목록에서 | 카드를 눌러 열거나, 골라서 한꺼번에 되돌립니다 |

**수금 전인 건을 숨기면 미수금 목록에서도 빠집니다.** 돈 문제라 숨기기 전에
그렇다고 말해 줍니다. 다만 금액을 못 보는 직원에게는 안 띄웁니다 —
직원 화면에는 금액 표가 안 내려와서 완료된 건이 전부 "수금 전" 으로 보입니다.

#### 여러 건 골라서

"묶어서 청구" 버튼 이름을 **"고르기"** 로 바꿨습니다. 하는 일이 하나가 아닙니다.

고르기 바를 두 줄로 나눴습니다 — 위는 세는 줄, 아래는 **숨기기 · 정산 · 청구서 · 지우기**.
한 줄에 넷을 넣으면 360px 폰에서 삐져나옵니다. 버튼은 폭을 똑같이 나눠 갖습니다.

- **숨김 목록에서 고르면** 숨기기가 **되돌리기**로 바뀌고, 정산·청구서는 숨깁니다.
  숨긴 것을 청구할 일은 없습니다
- **지우기** 는 고른 것을 한 번에 `deleted=true` 로 표시하고 공유 링크도 막습니다.
  단건 지우기와 같은 방식입니다. 몇 건인지 · 공유 링크 몇 개가 막히는지 ·
  수금 전인 건이 몇 개인지 세어 보여 준 뒤 묻습니다
- 스무 건을 골라도 서버에 한 번만 갑니다 (`.in('id', […])`)

#### 곁들여 고친 것

- **못 누르는 버튼이 눌릴 것처럼 보였습니다.** `.btn:disabled` 뒤에 `.btn.line` ·
  `.btn.ghost` · `.btn.danger` 가 와서 이기고 있었습니다. 넷이 나란히 서니
  청구서 하나만 회색이라 눈에 띄었습니다
- **숨긴 것을 복제하면 사본까지 숨어 있었습니다.** 새로 만든 것은 보여야 합니다

#### 확인한 것

브라우저에서 단건·여러 건 · 숨기기·되돌리기·지우기를 다 눌러 봤습니다.
360px 폭에서 버튼 넷이 76px씩 들어가고 글자가 안 잘립니다. 콘솔 오류는 없습니다.

- 숨기면 칩 숫자(전체 3 → 2) · 현황 · 미수금에서 함께 빠집니다
- 되돌렸을 때 숨긴 것이 하나도 안 남으면 목록 화면으로 데려옵니다.
  칩이 사라진 자리에 빈 화면만 남으면 어디를 보고 있었는지 알 수 없습니다
- 견적서는 숨길 수 없습니다 — 견적서는 이 상세 화면을 안 거칩니다

### 위쪽 Re:Bind 마크를 누르면 처음 화면 (2026-08-29)

설정이나 현황을 보다가 프로젝트 목록으로 돌아가려면 아래 탭을 찾아 눌러야 했습니다.
이제 위쪽 마크가 곧 홈 버튼입니다.

- 열린 것이 있으면 먼저 닫습니다. `topLayer()` 로 위에서부터 하나씩 —
  뒤로가기가 쓰는 것과 같은 차례라 닫는 순서가 어긋나지 않습니다
- **검색어와 걸러 둔 칩도 비웁니다.** "처음 화면" 인데 어제 쳐 둔 검색어가
  그대로 걸려 있으면 목록이 비어 보여서 고장 난 줄 압니다

**상세와 명세서에서는 안 눌립니다.** 그 두 화면은 자기 윗줄(`.dbar`)이 앱바를
덮습니다. 대신 거기에는 이미 `‹` 가 있어서 목록으로 돌아갑니다. 덮개를 걷어
마크를 내놓기보다 화면마다 돌아가는 길이 하나씩 있는 편이 낫다고 봤습니다.

`<div class="brand">` 를 `<button>` 으로 바꿨습니다. 버튼이 되면 브라우저가
테두리·바탕·글꼴을 제 마음대로 넣어서 그것부터 지웠습니다. 눌린 느낌만
`:active` 로 남겼습니다. 보이는 모습은 그대로입니다.

### Re:Bind 와 Re:Call 을 갈랐습니다 (2026-08-29)

**왜** — 두 앱이 한 데이터베이스, 한 계정 목록을 씁니다. 로그인 만드는 방식도
글자 그대로 같습니다(`아이디@회사코드.ndna.invalid`). 그래서 **제본소 직원이
recall.dnalabs.kr 에 같은 아이디로 그냥 들어갔습니다.** 그 회사의 Re:Call 이
비어 있어 빈 화면을 볼 뿐이었지만, 팀원 목록에는 제본소 사람들이 그대로 떴습니다.

#### 구독은 회사가 합니다 — `companies.apps`

`sql/0019_apps.sql` (적용 끝). 사람(`profiles`)이 아니라 회사에 답니다.

```
apps   {rebind} · {recall} · {rebind,recall}
```

| 회사 | | 왜 |
|---|---|---|
| ACTIVA | `{rebind,recall}` | 실제로 양쪽에 자료가 있습니다 (프로젝트 16 · 고객 3 · 활동 7) |
| BKT | `{rebind}` | Re:Call 자료가 없습니다 |

**기본값은 비워 뒀습니다.** 회사를 새로 열고 `apps` 를 안 채우면 아무도 못
들어갑니다. 반대로 `{rebind,recall}` 을 기본으로 두면 안 판 것까지 열립니다.
막히는 쪽이 낫습니다 — 막히면 바로 알아채지만, 열려 있는 것은 아무도 모릅니다.
⚠ **회사를 새로 만들 때 `apps` 를 반드시 함께 넣으세요.**

#### 화면이 아니라 데이터베이스가 막습니다

정책이 스무 개라 조건을 하나하나 늘려 쓰면 한 군데만 빠뜨려도 그 표가 열린 채로
남습니다. 그래서 함수 하나로 감쌌습니다.

```sql
company_for_app('rebind')   -- 산 서비스면 회사 id, 아니면 null
```

정책마다 `company_id = current_company_id()` 를 이것으로 바꿨습니다.
null 과의 비교는 참이 되지 않아 그대로 닫힙니다.

| Re:Bind | `projects` · `project_steps` · `project_money` · `client_favorites` |
|---|---|
| **Re:Call** | `customers` · `activities` |
| **공용** | `companies` · `profiles` · `company_settings` · `audit_log` |

**확인했습니다** — ACTIVA 를 잠깐 `{rebind}` 로 바꿔 보니 고객 3·활동 7 이
0 이 되고, `{}` 로 하니 프로젝트까지 0 이 됐습니다. 되돌리고 나면 그대로입니다.
(트랜잭션 안에서 하고 rollback 했습니다.)

로그인 직후에도 한 번 더 봅니다. 데이터베이스가 이미 막고 있지만, 막힌 채로
앱에 들어가 빈 화면을 보면 **자료가 사라진 줄 압니다.** 그래서 아예 못 들어오게
하고 "이 회사는 Re:Bind 를 쓰는 회사가 아닙니다" 라고 적어 줍니다.
Re:Call 을 산 회사면 그쪽으로 가라고 덧붙입니다.

#### 로그인 화면

- **DNA Labs 마크가 가운데 위로, 크게** (50 → 76px). 제품 이름은 아래 토글이
  말해 주므로 위에는 회사 이름만 둡니다 — 위에도 Re:Bind 를 적으면 고르기 전부터
  한쪽으로 정해 놓은 꼴이 됩니다
- **좌우 토글.** 두 칸이 그대로 누르는 자리입니다(로고 · 이름 · 두 줄 설명).
  뒤에서 판이 미끄러지고, 고른 쪽만 글자가 밝아지고 가운뎃점에 색이 들어옵니다
  (Re:Bind 파랑 · Re:Call 초록)
- **고른 것은 이 기기에 남습니다.** 다음에 와도 그 자리에서 시작합니다

**PC 는 두 칸으로 펼칩니다** (`min-width:900px` 이고 `min-height:600px` 일 때).
왼쪽에 마크와 고르는 자리, 오른쪽에 로그인 칸을 상자로 둡니다. 1440px 화면에
400px 한 줄만 세워 두면 양옆이 520px씩 비기 때문입니다.

토글은 좌우로 갈라 놓지 않고 **한 덩어리로 왼쪽에** 둡니다 — 갈라 놓으면
"둘 중 하나" 라는 것이 안 보입니다.

가로만 보지 않고 세로도 함께 봅니다. 가로만 보면 노트북 창을 반으로 줄여
세로로 긴 창을 만들었을 때도 두 칸이 되어 오히려 답답합니다.
900px 아래에서는 지금까지의 한 줄로 돌아갑니다.

**세로가 짧은 노트북** — 마크를 키우고 토글을 넣으면서 칸이 752px 이 되었습니다.
1366×768 노트북의 실제 높이는 700 안팎이라 그대로는 넘쳤고, `align-items:center`
때문에 **위로 삐져나간 마크를 스크롤로도 되돌릴 수 없었습니다.** 가운데 모으는
일을 `margin:auto` 로 바꾸고(그 자리를 남겨 둡니다), `max-height:790px` 에서는
마크와 여백을 조금씩 줄여 667px 에 들어가게 했습니다. 폰(844px)은 그대로입니다.
줄이는 것은 **한 칸일 때만** 합니다 — 두 칸으로 펼치면 1366×700 에서도 511px 이라
줄일 이유가 없고, 그런데도 줄이면 왼쪽 마크만 작아져 눌린 것처럼 보입니다.

**고르자마자 넘어가지는 않습니다.** 두 앱은 주소가 달라서(rebind/recall.dnalabs.kr)
로그인한 흔적을 넘겨줄 수 없습니다. 그래서 Re:Call 을 고르면 로그인 칸 대신
"Re:Call 은 다른 주소에서 엽니다" 와 **열기** 단추가 나옵니다. 바로 보내 버리면
잘못 눌렀을 때 하던 것이 날아가고, 무엇보다 **Re:Call 을 골라 둔 줄 모르고
여기에 비밀번호를 치는 일이 없어야 합니다.**

#### Re:Call 쪽도 붙였습니다 (2026-08-29)

`~/Desktop/network-dna/network-dna.html` **한 파일만** 고쳐 올렸습니다.
로그인 확인 · 좌우 토글 · 큰 마크 · PC 두 칸 — Re:Bind 와 같은 모양입니다.
**좌우 차례도 같습니다**(왼쪽 Re:Bind · 오른쪽 Re:Call) — 자리가 바뀌면
같은 데를 눌렀는데 다른 것이 골라집니다.

토글이 기억하는 열쇠말(`dnalabs-app`)은 두 앱이 같은 이름을 쓰지만 주소가
달라 **서로 다른 칸에 담깁니다.** 넘겨줄 방법은 없습니다 — 한쪽에서 고른
것이 다른 쪽에 옮겨 가지 않습니다.

⚠ **그 저장소에는 아직 안 올라간 결제 시스템이 남아 있습니다** —
`0003_billing.sql` · `signup`/`subscription` 서버 함수 · `catalog.json` ·
홈페이지 `web/`. 이번에 **일부러 건드리지 않았습니다.** 다만
`network-dna.html` 안에 이미 들어 있던 **요금제 화면**은 파일이 하나라
함께 올라갔습니다. `subscriptions` 표가 아직 없어서 그 카드는 스스로
숨습니다(`loadSub` 이 오류면 그냥 돌아가고 `renderSub` 이 감춥니다).

#### 예전 메모 — 그때는 안 했습니다

**Re:Call 파일은 건드리지 않았습니다.** 그 저장소에 **아직 안 올라간 결제
시스템**이 통째로 들어 있습니다 — `0003_billing.sql`(구독·좌석제한·회사코드
자동발급) · `signup`/`subscription` 서버 함수 · 요금제 `catalog.json` ·
홈페이지 `web/`. 지금 제가 그 저장소에 손대면 **그 미완성 작업까지 함께
올라갑니다.** 데이터베이스에도 아직 적용 전입니다.

그래서 Re:Call 쪽에는 토글도, 로그인 확인도 없습니다. 다만 **자료는 이미
막혀 있습니다** — 제본소 사람이 Re:Call 에 들어가도 고객과 활동이 한 줄도
안 보입니다. 그 저장소를 손대도 될 때 알려 주시면 같은 것을 붙이겠습니다.

#### 결제를 붙일 때

Re:Call 의 `subscriptions` 설계는 **회사당 한 줄**(`company_id primary key`)이라
"무엇을 샀는가" 를 담지 못합니다. 결제를 붙일 때 그 표를 `(company_id, product)`
로 넓히고, 결제가 성사되면 `companies.apps` 에 그 서비스를 더하도록 이으면 됩니다.
지금 `apps` 를 `subscriptions` 에서 끌어오게 만들지 않은 것은, 그 표가 아직
없고 설계도 Re:Call 것 하나뿐이기 때문입니다.

**한 회사 안에서 사람마다 가르는 것**(제본 직원에게 영업 고객을 감추기)은
아직 안 했습니다. 그건 사람 단위 칸이 하나 더 있어야 합니다.

### 한 주소 아래로 — 한 번 로그인하면 둘 다 (2026-08-30)

**왜** — 로그인 화면에 두 서비스를 나란히 놓았지만, **거기서 로그인해도
반대쪽은 몰랐습니다.** 브라우저가 로그인한 흔적을 주소마다 따로 보관하기
때문입니다. `rebind.dnalabs.kr` 의 흔적을 `recall.dnalabs.kr` 이 읽는 것은
브라우저가 막습니다 — 그래야 아무 사이트나 남의 로그인을 훔쳐보지 못합니다.

그래서 **주소를 하나로 모읍니다.**

```
dnalabs.kr/bind   →  Re:Bind
dnalabs.kr/call   →  Re:Call
```

파일은 두 저장소에 그대로 둡니다. `dnalabs.kr` 이 이미 Vercel 이라
그 주소로 **비춰 주기만** 합니다(rewrite — 주소는 안 바뀌고 내용만 가져옵니다).
GitHub Pages 배포 방식도 그대로입니다. 주소가 하나가 되니 흔적을 나눠 쓰고,
**한 번 로그인하면 둘 다 열립니다.**

#### 앱이 자기가 어디 있는지 스스로 봅니다

```js
const underOneRoof = ONE_ROOF[APP_KEY] === location.pathname.replace(/\/+$/,'');
```

| 어디서 열렸나 | 반대쪽을 고르면 |
|---|---|
| `dnalabs.kr/bind` | **로그인 칸이 그대로 있습니다.** 여기서 로그인하고 그쪽으로 넘어갑니다 |
| `rebind.dnalabs.kr` | 칸을 감추고 이동 단추만. 흔적을 못 넘기니 **엉뚱한 앱에 비밀번호를 치면 안 됩니다** |

경로는 못 박아 뒀습니다. 짐작으로 맞히려 들면 `/bindery.html` 같은 것까지
한 지붕으로 잘못 보고, 그때부터 있지도 않은 `/call` 로 사람을 보냅니다.

#### 갈림길 넷을 다 확인했습니다

| 상황 | 결과 |
|---|---|
| 둘 다 산 회사 · Re:Call 골라 로그인 | `/call` 로 넘어갑니다 |
| Re:Call 만 산 회사가 `/bind` 로 로그인 | **내쫓지 않고** `/call` 로 보냅니다 |
| Re:Bind 만 산 회사가 Re:Call 을 골라 로그인 | 토글을 되돌리고 "Re:Bind 로 들어가시면 됩니다" |
| 옛 주소 | 지금까지와 똑같습니다 |

설정에도 **옆 앱으로 가는 단추**를 뒀습니다. 산 앱일 때만 나옵니다 —
안 산 앱으로 보내면 그쪽에서 막히고 돌아오지도 못합니다.
옛 주소에서는 "그쪽에서 한 번 더 로그인해야 합니다" 라고 적어 둡니다.

#### 서버는 준비됐습니다

`ALLOWED_ORIGIN` 에 `https://dnalabs.kr` 을 **더하고**(옛 주소 둘은 그대로)
함수 넷을 다시 배포했습니다 — `share-view` · `read-order` · `read-card` ·
`admin-user`. 세 주소 다 통과하고 모르는 주소는 그대로 막힙니다.

#### 올렸습니다 (2026-08-30 14:45)

`web/vercel.json` 에 `rewrites` 를 넣고 `npx vercel --prod` 로 배포했습니다.
`dnalabs.kr/bind` · `/call` 이 열립니다.

**확인한 것**

| | |
|---|---|
| rewrite 인가 | 최종 주소가 `dnalabs.kr/bind` 그대로입니다. redirect 였으면 옛 주소로 튕깁니다 |
| 내용 | `/bind` → `Re:Bind · 제조 공정과 거래명세서`, `/call` → `Re:Call · 고객관리` |
| **로그인 나눠 쓰기** | `/bind` 에서 보관함에 쓴 값을 `/call` 이 그대로 읽습니다. **주소가 하나라는 증거입니다** |
| 서버 함수 | `dnalabs.kr` 에서 `share-view` 를 불러 응답을 받았습니다. 모르는 주소는 그대로 막힙니다 |
| 옛 주소 | 둘 다 200. 지금까지와 같습니다 |

**Vercel CLI 는 `--scope` 를 붙여야 했습니다.** `.vercel/project.json` 의
`orgId` 와 로그인한 계정의 팀 이름이 달라 그냥 돌리면 `Not authorized` 가 납니다.

```bash
cd ~/Desktop/network-dna && npx vercel --prod --scope chhanj40-5991s-projects
```

#### ⚠ `ALLOWED_ORIGIN` 이 한 번 어긋났습니다

처음 `secrets set` 을 돌렸을 때 **값이 제대로 안 들어갔습니다.** 바로 뒤 확인에서는
통과했는데 나중에 다시 보니 `dnalabs.kr` 이 빠져 있었고, 저장된 값의 해시가
제가 넣으려던 문자열과 달랐습니다. 원인은 못 찾았습니다.

다시 넣고 함수 넷을 재배포한 뒤, **해시를 직접 대조해** 맞는 것을 확인했습니다.

```
9e3e2b13… = sha256("https://rebind.dnalabs.kr,https://recall.dnalabs.kr,https://dnalabs.kr")
```

**이 값을 고칠 때는 넣고 끝내지 말고 반드시 되읽어 확인하세요.** 틀려도 조용히
넘어가고, 앱은 로그인 화면까지는 멀쩡히 뜨다가 서버를 부를 때만 막힙니다.

```bash
# 지금 무엇이 통과하는지 밖에서 확인하는 법
curl -s -o /dev/null -D - -X OPTIONS \
  https://izrtclsqhsgkuwsffifn.supabase.co/functions/v1/share-view \
  -H "Origin: https://dnalabs.kr" -H "Access-Control-Request-Method: POST" \
  | grep -i access-control-allow-origin
# → 부른 주소가 그대로 돌아오면 통과, 다른 주소가 오면 막힌 것입니다
```

#### 예전 메모 — Vercel 을 올리기 전

#### 남은 것 — Vercel 은 직접 올리셔야 합니다

`dnalabs-vercel/` 에 넣어 뒀습니다. 제가 그 프로젝트에 닿을 수 없습니다.

| `vercel.json` | `rewrites` 두 줄. **기존 `headers`·`cleanUrls` 는 지우지 마세요** |
|---|---|
| `bind.webmanifest` | 홈 화면 설치용. `web/` 맨 위에 두면 `/bind.webmanifest` 로 열립니다 |
| `README.md` | 올리는 법과 안 될 때 볼 곳 |

⚠ **옛 주소를 끄지 마세요.** 고객사 공유 링크(`?t=`)가 이미 그 주소로 나가
있고, Vercel 이 그 주소에서 내용을 가져오므로 끄면 새 주소도 함께 멈춥니다.

### 다음에 할 만한 것

| | 왜 |
|---|---|
| **Re:Call 저장소 정리** | 8월 27일 분리분이 안 올라가 있습니다. 위 📌 참고 |
| **작업의뢰서 실기 확인** | 로그인이 필요해 제가 못 합니다. 샘플을 폰으로 찍어 어느 칸이 틀리는지 알려 주시면 지시문을 조입니다 |
| **공급자 정보 진짜 값** | 대표자·주소가 아직 예시입니다 |
| ~~금액을 진짜로 잠그기~~ | 2026-08-28 에 했습니다 (옛 칸까지 지웠습니다) |
| ~~`ALLOWED_ORIGIN` 잠그기~~ | 2026-08-28 에 했습니다 |
| ~~도메인 사서 연결~~ | 2026-08-28 에 했습니다 — https://rebind.dnalabs.kr/ |
| ~~안드로이드 실행 아이콘~~ | 2026-08-28 에 했습니다 (manifest) |

### 남은 것 — 하나뿐입니다

**앱 → 설정 → 공급자 정보** 를 관리자 계정으로 채워 주세요.
사업자등록번호·대표자·주소·업태·종목·계좌가 거래명세서 왼쪽 "공급자" 칸에 그대로 들어갑니다.
비워 두면 명세서의 그 칸이 빕니다.

~~테스트가 끝나면 `ALLOWED_ORIGIN` 에서 `localhost` 를 빼고~~ **2026-08-28 에 잠갔습니다.**
지금은 `https://motodna.github.io` 에서만 부를 수 있습니다. 다른 곳에서 부르면
브라우저가 막습니다 (확인했습니다). **주소를 옮기면 여기도 같이 바꿔야 합니다.**

```bash
supabase secrets set ALLOWED_ORIGIN="https://새주소" --project-ref izrtclsqhsgkuwsffifn
supabase functions deploy share-view --no-verify-jwt --project-ref izrtclsqhsgkuwsffifn
```

### DNA Labs 마크를 다시 그렸습니다

전 마크는 **DNA 가 중심보다 위에 있고 LABS 가 너무 작아** 상단 바 크기(26px)에서
LABS 가 읽히지 않았습니다. 다시 그렸습니다.

| | |
|---|---|
| **DNA** | 원의 한가운데. 글자 윗선과 밑선의 중간이 원의 중심에 오게 기준선을 맞췄습니다 |
| **LABS** | 그 아래 빈 공간을 채웁니다. 자간을 벌려 DNA 폭에 가깝게 |
| **크기** | 26px 에서도 LABS 가 읽힙니다 (확인) |

`make-logo.py` 가 그림을 그리고 `bindery.html` 세 곳(홈 화면 아이콘 · 탭 아이콘 · 화면 안 로고)에
바로 넣어 줍니다. 값을 고치고 다시 돌리면 됩니다.

```bash
cd /Users/motodna/Desktop/Rebind
python3 make-logo.py          # logo.png 를 만들고 앱에도 넣습니다
```

여섯 배로 그린 뒤 줄이기 때문에 가장자리가 부드럽습니다.
글꼴은 맥에 있는 Helvetica Neue Bold 를 씁니다. Pretendard 는 설치돼 있지 않은데,
대문자 네 글자라 눈으로는 차이가 없습니다.

**이 마크는 Re:Bind 것입니다.** Re:Call 은 예전 마크를 그대로 씁니다.
잠깐 양쪽을 맞췄다가 되돌렸습니다 — `network-dna.html` 은 손대지 않은 상태입니다.

두 앱이 한 회사의 것이지만 마크를 나눠 두면, 고객사가 링크를 열었을 때
"제조 쪽 화면"이라는 것이 한눈에 구분됩니다.

---

## 정한 것들과 그 이유

**고객사에게 계정을 주지 않고 링크로 열었습니다.**
계정을 주면 담당자가 바뀔 때마다 발급·정지를 해야 합니다. 링크는 새어 나가면
새로 만들어 앞의 것을 그 자리에서 막습니다. 대신 링크로 보이는 것은 읽기뿐이고,
프로젝트 하나로 한정됩니다.

**기기에 담아 두지 않고 서버를 바로 읽고 씁니다.**
Re:Call 은 밖에서 쓰는 앱이라 기기에 먼저 담고 나중에 맞췄지만, 여기서는
고객사가 링크를 여는 그 순간의 값이 맞아야 합니다. 그래서 서버가 원본입니다.

**PDF 를 따로 만들지 않고 브라우저 인쇄를 씁니다.**
직접 만들면 한글 글꼴을 파일에 넣어야 하고(수 MB), 줄바꿈이 깨집니다.
인쇄는 글꼴이 그대로 나오고 A4 여백을 브라우저가 맞춰 줍니다.
아이폰·안드로이드·PC 모두 "PDF로 저장"이 있습니다.

**엑셀은 열 때만 내려받습니다.** 쓰지 않는 사람에게 400KB 를 미리 물리지 않으려고요.

---

## 자주 쓸 명령

```bash
cd /Users/motodna/Desktop/Rebind

# 로컬에서 보기 → http://localhost:8791/bindery.html
python3 -m http.server 8791

# 서버 함수 배포 (--no-verify-jwt 를 꼭)
supabase functions deploy share-view --no-verify-jwt --project-ref izrtclsqhsgkuwsffifn

# 부를 수 있는 주소 잠그기 (바꾼 뒤에는 다시 배포해야 반영됩니다)
supabase secrets set ALLOWED_ORIGIN="https://motodna.github.io,http://localhost:8791" \
  --project-ref izrtclsqhsgkuwsffifn
```

## 파일 구성

```
bindery.html                     앱 본체 (직원 화면 + 고객사 화면)
make-logo.py                     DNA Labs 마크를 그려 앱에 넣습니다
logo.png                         만들어진 마크 (180×180)
sql/0003_works.sql               표 · 접근 규칙 · 사진 보관함
supabase/functions/share-view/   고객사 공개 링크를 읽어 주는 서버 함수
supabase/functions/_shared/      어디서 부를 수 있는지 (Re:Call 것과 같은 파일)
supabase/project.json            접속 주소와 공개 키
설치방법.md                       설치 절차 10분
README.md                        무엇을 하는 앱인지
```
