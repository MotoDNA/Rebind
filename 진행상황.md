# Re:Bind — 진행 상황

제조 공정과 거래명세서 · DNA Labs

마지막 정리: 2026-08-27

## 링크

| | |
|---|---|
| **앱 (운영)** | https://motodna.github.io/Rebind/ |
| **저장소** | https://github.com/MotoDNA/Rebind |
| **Supabase 프로젝트** | https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn |
| **함수 로그** | https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn/functions |
| **SQL 편집기** | https://supabase.com/dashboard/project/izrtclsqhsgkuwsffifn/sql/new |

로그인은 **Re:Call 과 같은 회사 코드·아이디·비밀번호** 입니다.

---

## 무엇을 만들었나

제본소가 만드는 것을 **공정 단위로 기록**하고, 고객사 담당자는 **링크만 열어** 지금
어디까지 갔는지 봅니다. 공정이 끝나면 그동안 쌓인 숫자가 **그대로 거래명세서**가 됩니다.

Re:Call 과 **같은 Supabase 프로젝트, 같은 계정**을 씁니다. 계정을 따로 만들지 않습니다.

```
Re:Call  network-dna.html   고객 · 명함 · 미팅 타임라인   (~/Desktop/network-dna)
Re:Bind  bindery.html       프로젝트 · 공정 · 명세서       (~/Desktop/Rebind)
              └── 같은 데이터베이스 · 같은 로그인
```

## 만든 순서 — 고객에게 설명할 때

| | 무엇을 | 왜 그 순서인가 |
|---|---|---|
| **1. 그릇** | 데이터베이스 표 3개와 접근 규칙 | 그릇 모양은 나중에 바꾸기가 가장 어렵습니다. 이미 쌓인 기록을 다 옮겨야 하니까요 |
| **2. 문지기** | 서버 함수 `share-view` | 고객사는 계정이 없습니다. 링크의 열쇠를 확인하고 그 프로젝트 하나만 꺼내 줍니다 |
| **3. 화면** | `bindery.html` | 넣는 쪽(직원)과 보는 쪽(고객사)이 한 파일에 들어 있습니다 |
| **4. 종이** | 거래명세서 | 다시 입력하지 않습니다. 쌓인 것이 그대로 종이가 됩니다 |

뒤로 갈수록 고치기 쉽습니다.

---

## 지금 상태

### 다 된 것 — 코드

- **표 3개와 접근 규칙** — `sql/0003_works.sql`
  - `projects` 프로젝트 하나 = 한 줄 (사양 · 금액 · 일정 · 공유 토큰)
  - `project_steps` 공정 기록 (공정명 · 전체에서 몇 % · 일자 · 메모 · 사진)
  - `company_settings` 명세서에 찍히는 우리 회사 정보 (회사마다 한 줄, 관리자만 고침)
  - `works` 사진 보관함 — 경로가 `{회사id}/{프로젝트id}/…` 라 폴더 이름이 곧 권한
  - 규칙은 Re:Call 의 고객 규칙을 그대로 본떴습니다 (관리자 · 담당 · 공유받은 사람)
- **서버 함수 `share-view`** — 토큰을 확인하고 그 프로젝트 하나만 돌려줍니다.
  내부 메모 · 담당자 id · 다른 프로젝트는 나가지 않습니다. 사진은 한 시간짜리 주소로만.
  1분에 60번을 넘겨 부르면 늦춥니다.
- **앱 `bindery.html`** — 파일 하나. 주소에 `?t=…` 가 붙어 있으면 고객사 화면으로,
  없으면 직원 화면으로 스스로 갈립니다.
  - 프로젝트 목록 (상태 칩 · 검색 · 급한 것이 위로)
  - 공정 타임라인, 제본소에서 흔한 순서 11개를 눌러서 채우기
  - **알아서 바뀌는 상태** — 첫 기록이 올라가면 진행 중, 100% 면 완료 + 납품일 기록
  - 제조 상세 · 금액(추가 품목 포함) · 완성본 사진 · 내부 메모
  - 현황 탭 — 납기 지난 건 · 이번 주 납기 · 최근 이레의 공정
  - 거래명세서 A4, **PDF(인쇄) · 엑셀(.xlsx) · 링크**
  - 25초마다 조용히 다시 받아 옵니다. 실시간 알림이 오면 그때 바로.
    고치는 중(시트가 열려 있을 때)에는 건드리지 않습니다.

### 화면으로 확인한 것

로컬에서 가짜 자료를 넣어 그려 보았습니다.

- 목록 · 상태 칩 · 진척 막대 — 정상
- 상세 · 공정 타임라인 · 제조 상세 — 정상
- 금액 셈 — 3,000부 × 1,850원 + 추가 150,000원 = 공급가 5,700,000 / 부가세 570,000 / 합계 6,270,000 ✓
- 거래명세서 A4 — 공급자/공급받는자, 품목표, 사양, 공정 내역(총 소요일 16일), 계좌·안내문 ✓
- 고객사 공개 화면 — 진행도 · 공정 · 사양 · 금액 ✓

### 설치 — 끝났습니다

| | | |
|---|---|---|
| 표와 접근 규칙 | `sql/0003_works.sql` | 표 3개 · 규칙 11개 · `works` 보관함(규칙 4개) 확인 |
| 서버 함수 | `share-view` | 배포 완료. 로그인 없이 불러지고 `motodna.github.io` 만 허용 |
| 앱 올리기 | GitHub Pages | https://motodna.github.io/Rebind/ |

**실제 링크로 왕복 확인했습니다.** 시험용 프로젝트를 하나 넣고 공유 링크를
로그인하지 않은 상태에서 열어, 진행도 45% · 공정 세 줄 · 사양 · 금액이 나오는 것과
**내부 메모가 나가지 않는 것**을 확인한 뒤 지웠습니다. 지금 프로젝트는 0건입니다.
껐던 토큰으로 다시 부르면 404 가 돌아옵니다.

### 권한 · 서류 · 견적서 (2026-08-27 저녁)

**관리자와 직원이 보는 것이 달라졌습니다.**

| | 관리자 | 직원 |
|---|---|---|
| 프로젝트 · 공정 기록 · 완성본 사진 | 봅니다 | 봅니다 |
| 금액 · 내부 메모 | 봅니다 | **안 보입니다** |
| 고객사 공유 · 거래명세서 · 청구서 · 견적서 | 봅니다 | **안 보입니다** |
| 아래 탭 | 5개 (견적서 포함) | 4개 |

> ⚠ **이건 화면에서 가리는 것입니다.** 데이터베이스는 여전히 회사 사람 모두에게
> 금액 칸을 내려 줍니다. 개발자 도구를 열 줄 아는 직원이라면 볼 수 있습니다.
> 진짜로 막으려면 금액을 딴 표로 빼고 관리자만 읽게 규칙을 걸어야 합니다.
> 하루쯤 걸립니다. 필요하시면 말씀해 주세요.

**서류가 셋이 됐습니다.** 담는 값은 같고 종이 모양만 다릅니다.

- **거래명세서** — 납품하고 나서. 공정 내역과 완성본 사진이 함께
- **청구서** — 제본소에서 쓰던 종이 양식 그대로. 줄마다 세액, 총합계금액을 한글로
- **견적서** — 청구서와 같은 틀. "견적금액" 과 유효기간 30일 안내

**견적서 탭이 생겼습니다.**

- 입력 칸은 새 프로젝트와 같습니다. 폼을 두 벌 두면 한쪽만 고치는 실수가 나서 하나를 돌려 씁니다
- 저장한 견적서를 누르면 종이가 뜨고, 거기서 **고치기** 와 **프로젝트로 옮기기**
- 새 프로젝트 화면 맨 위의 **견적서에서 불러오기** 로도 끌어옵니다

**고객사 칸을 누르면 최근 다섯 곳이 뜹니다.** 고르면 담당자·연락처는 물론
**지난번 사양(규격·색상·표지/내지 용지·제본·후가공·단가)까지** 그대로 채웁니다.
수량과 발주번호만 비워 둡니다 — 그건 매번 다르니까요.

**용지를 표지와 내지로 나눴습니다.** 예전에 한 칸에 몰아 적어 둔 것은 그대로 보입니다.

**제조 상세에 칸을 직접 더할 수 있습니다.** 칸 이름부터 적습니다. 링 색상, 귀도리,
포장 방법처럼 품목마다 다른 것들을 위해서입니다.

### 월말에 한 거래처로 몰아 청구하기

프로젝트 목록에서 **묶어서 청구** 를 누르면 고르기가 켜집니다.
카드를 **길게 눌러도** 켜집니다.

1. 검색칸에 거래처 이름을 넣어 그 곳 것만 걸러 놓고
2. 카드를 눌러 고르면 (또는 **전체**)
3. 아래 띠에 **몇 건 · 합계 · 거래처 · 몇 월분** 이 뜨고
4. **청구서** 를 누르면 한 장에 **날짜별로** 적힙니다

품목 표에 **날짜 칸이 하나 늘어납니다.** 납품일 순으로 서고, 한 프로젝트에
박스·용달비 같은 줄이 딸려 있으면 그 줄은 날짜를 비워 그 건에 속한 것으로 보입니다.

**거래처가 섞이면 만들지 않습니다.** 청구서는 한 곳으로 나가는 종이라
섞인 곳 이름을 알려 주고 멈춥니다. 검색으로 한 곳씩 걸러 주세요.

엑셀로 내려받아도 날짜·품명·수량·단가·공급가액·세액·합계가 그대로 나옵니다.

### 정산 — 청구서 발송 · 세금계산서 · 수금

프로젝트마다 세 가지를 따로 체크합니다. **켜고 끄는 값이 아니라 날짜로 둡니다.**
"했다/안 했다" 만 남기면 "언제 청구했더라", "이거 며칠째 안 들어오지" 를 셀 수 없습니다.
켜면 오늘 날짜가 박히고, 옆 칸에서 다른 날로 고칠 수 있습니다.

| 어디에 | 무엇이 |
|---|---|
| **프로젝트 상세 → 정산** | 세 줄. 켠 것은 날짜가, 안 켠 것은 "아직" 이 뜹니다.<br>수금이 안 됐으면 **"납품하고 21일째 못 받았습니다"** 가 붙습니다 |
| **목록 카드** | `청` `세` `수` 세 글자. 끝난 것만 초록으로 |
| **목록 칩** | **미수금** 칩이 하나 늘었습니다. 누르면 못 받은 것만 |
| **현황** | 미수금 합계와 건별 경과 일수. 오래된 것부터 위로 |
| **청구서 화면** | **"이 n건을 청구 완료로 표시"** — 묶음 청구서를 뽑고 그 자리에서 한꺼번에 |

뽑는 것만으로 자동으로 찍지는 않습니다. 미리 보기만 하고 안 보내는 일도 있으니까요.

관리자만 봅니다. 직원 화면에는 정산 칸도, 청·세·수 표시도 없습니다.

### BKT 미팅 반영 (2026-08-27 밤)

| 요청 | 한 것 |
|---|---|
| 거래처 즐겨찾기 | 고객사 자동완성에서 별을 답니다. 별 단 곳이 맨 위로.<br>**사람 단위**라 내가 단 것은 나만 보이고, 기기를 바꿔도 따라옵니다 |
| 표지/내지 용지 구분 | **이미 되어 있었습니다** (8/27 낮 반영분) |
| 제조 상세 직접 항목 추가 | **이미 되어 있었습니다** — "그 밖의 항목" 에서 칸 이름부터 적습니다 |
| 검색하면 필터 숫자도 바뀌게 | 칩 숫자가 검색 결과를 셉니다. 거래처를 찾으면 그 곳 것만 몇 건인지 바로 보입니다 |
| 이미 청구한 건 재청구 확인 | 한 건이면 "이미 청구한 건입니다 (8.8 발송). 계속하시겠습니까?"<br>여러 건이면 "이미 청구한 프로젝트가 2개 있습니다. 계속 하시겠습니까?" |
| 여러 건 정산 체크 | 아래 띠에 **정산** 버튼. 청구서·세금계산서·수금을 골라 한 번에 표시하거나 지웁니다 |
| PC 화면이 불편함 | 아래 참고 |

### PC 화면

폰에 맞춘 한 줄짜리 화면(540px)을 그대로 키우니 가운데만 길쭉하고 양옆이 비었습니다.
**화면이 1000px 보다 넓어지면** 이렇게 바뀝니다.

- 폭이 1080px 로 넓어집니다
- 목록·현황·상세·입력 폼이 **두 줄로** 섭니다. 새 프로젝트는 스크롤 없이 한 화면에 다 들어옵니다
- 시트가 아래에서 올라오지 않고 **가운데 대화상자**로 뜹니다
- 거래명세서 A4 도안(794px)이 **줄어들지 않고 그대로** 들어갑니다
- 마우스를 올리면 짚어 줍니다

폰에서는 지금까지와 똑같습니다. 같은 파일 하나입니다.

### 작업의뢰서를 찍으면 프로젝트가 만들어집니다 (2026-08-27 밤)

**새 프로젝트 → 작업의뢰서 촬영.** 사진을 찍으면 서버가 표를 읽어 칸을 채웁니다.

받은 샘플 7장으로 맞춰 두었습니다. 양식이 여섯 가지나 되는데(솜씨컴퍼니·위드프린팅·
ezette·제일미디어 등) 칸 이름이 제각각이라 **위치가 아니라 뜻으로 찾게** 했습니다.

| 잘 읽히는 것 | 사람이 봐야 하는 것 |
|---|---|
| 제품명 · 수량 · 규격 · 면수 · 도수 | **고객사** — "발주처" 와 "입고처 · 제책사" 를 헷갈립니다 |
| 표지/내지 용지 · 제본 방식 · 후가공 | **납기일** — 발주일과 입고예정일이 나란히 있으면 헷갈립니다 |
| 담당자 · 연락처 · 발주번호 | **단가** — 의뢰서에 아예 없습니다. 직접 넣으셔야 합니다 |

확실하지 않은 칸은 **노란색으로 표시**되고 위에 무엇을 봐야 하는지 적힙니다.
**바로 저장되지 않습니다.** 눈으로 보고 "만들기" 를 눌러야 들어갑니다.

원본 사진은 프로젝트에 붙어 남습니다(상세 → 작업의뢰서). 나중에 "몇 부라고 했더라"
할 때 대조합니다. **완성본 사진과 따로 둡니다** — 그건 명세서와 고객사 화면에 나가니까요.

샘플로 맞추다 실제로 잡은 것 하나: **육공타공을 스프링제본으로 잘못 읽었습니다.**
구멍만 뚫는 일과 링을 끼우는 일은 다른데 둘 다 "타공" 이 들어가서입니다.
"링을 끼운다는 말이 없으면 타공이라고만 적어라" 로 고쳤습니다.

하루 200장까지, 장당 10원 안팎입니다. Re:Call 이 쓰던 API 열쇠를 그대로 씁니다.

### 자재 입고 (2026-08-27 밤)

고객사가 인쇄를 마친 종이를 BKT 로 보냅니다. 표지·내지·후표지가 따로 오고
다 들어와야 공정을 시작할 수 있습니다. 그래서 **입고 카드를 공정 위에** 놓았습니다.

- 프로젝트 상세 → **입고**. 자재마다 줄 하나, 들어오면 켭니다 (날짜가 박히고 고칠 수 있습니다)
- 처음이면 **"사양에서 가져오기"** — 적어 둔 표지/내지 용지로 줄을 만듭니다
- 후표지 용지 · 면지 · 띠지 · 간지 · 스프링(링) · 삼각대 · 박스는 눌러서 더합니다
- 아직 안 들어온 게 있으면 **공정 카드에 "아직 안 들어온 자재가 2개 있습니다"**
- 목록 카드에 `입고 1/3`, 현황에 **입고 대기** 묶음

표를 새로 만들지 않고 칸 하나(jsonb)에 담았습니다. 품목이 프로젝트마다 다르고
항상 그 프로젝트와 함께만 읽기 때문입니다.

### 단위 고르기 · 사진은 촬영과 앨범 둘 다

**단위**를 골라 넣습니다 — 부 · 권 · 장 · 세트 · 기타 · **직접입력**.
직접입력을 고르면 아래에 칸이 열립니다. 예전에 "매" 처럼 적어 둔 것이 있으면
열었을 때 자동으로 직접입력에 그 값이 들어가 있습니다. 새 프로젝트와 제조 상세 양쪽 다.

**사진**은 누르면 **촬영하기 / 앨범에서 고르기**를 먼저 묻습니다.
폰에 따라 사진 칸을 그냥 누르면 곧장 카메라만 열리는 것이 있어 두 길을 따로 두었습니다.
작업의뢰서와 완성본 사진 둘 다 그렇습니다.

### 남은 것 — 하나뿐입니다

**앱 → 설정 → 공급자 정보** 를 관리자 계정으로 채워 주세요.
사업자등록번호·대표자·주소·업태·종목·계좌가 거래명세서 왼쪽 "공급자" 칸에 그대로 들어갑니다.
비워 두면 명세서의 그 칸이 빕니다.

테스트가 끝나면 `ALLOWED_ORIGIN` 에서 `localhost` 를 빼고 함수를 다시 배포해 잠급니다.

```bash
supabase secrets set ALLOWED_ORIGIN="https://motodna.github.io" --project-ref izrtclsqhsgkuwsffifn
supabase functions deploy share-view --no-verify-jwt --project-ref izrtclsqhsgkuwsffifn
```

### DNA Labs 마크를 다시 그렸습니다

전 마크는 **DNA 가 중심보다 위에 있고 LABS 가 너무 작아** 상단 바 크기(26px)에서
LABS 가 읽히지 않았습니다. 다시 그렸습니다.

| | |
|---|---|
| **DNA** | 원의 한가운데. 글자 윗선과 밑선의 중간이 원의 중심에 오게 기준선을 맞췄습니다 |
| **LABS** | 그 아래 빈 공간을 채웁니다. 자간을 벌려 DNA 폭에 가깝게 |
| **크기** | 26px 에서도 LABS 가 읽힙니다 (확인) |

`make-logo.py` 가 그림을 그리고 `bindery.html` 세 곳(홈 화면 아이콘 · 탭 아이콘 · 화면 안 로고)에
바로 넣어 줍니다. 값을 고치고 다시 돌리면 됩니다.

```bash
cd /Users/motodna/Desktop/Rebind
python3 make-logo.py          # logo.png 를 만들고 앱에도 넣습니다
```

여섯 배로 그린 뒤 줄이기 때문에 가장자리가 부드럽습니다.
글꼴은 맥에 있는 Helvetica Neue Bold 를 씁니다. Pretendard 는 설치돼 있지 않은데,
대문자 네 글자라 눈으로는 차이가 없습니다.

**이 마크는 Re:Bind 것입니다.** Re:Call 은 예전 마크를 그대로 씁니다.
잠깐 양쪽을 맞췄다가 되돌렸습니다 — `network-dna.html` 은 손대지 않은 상태입니다.

두 앱이 한 회사의 것이지만 마크를 나눠 두면, 고객사가 링크를 열었을 때
"제조 쪽 화면"이라는 것이 한눈에 구분됩니다.

---

## 정한 것들과 그 이유

**고객사에게 계정을 주지 않고 링크로 열었습니다.**
계정을 주면 담당자가 바뀔 때마다 발급·정지를 해야 합니다. 링크는 새어 나가면
새로 만들어 앞의 것을 그 자리에서 막습니다. 대신 링크로 보이는 것은 읽기뿐이고,
프로젝트 하나로 한정됩니다.

**기기에 담아 두지 않고 서버를 바로 읽고 씁니다.**
Re:Call 은 밖에서 쓰는 앱이라 기기에 먼저 담고 나중에 맞췄지만, 여기서는
고객사가 링크를 여는 그 순간의 값이 맞아야 합니다. 그래서 서버가 원본입니다.

**PDF 를 따로 만들지 않고 브라우저 인쇄를 씁니다.**
직접 만들면 한글 글꼴을 파일에 넣어야 하고(수 MB), 줄바꿈이 깨집니다.
인쇄는 글꼴이 그대로 나오고 A4 여백을 브라우저가 맞춰 줍니다.
아이폰·안드로이드·PC 모두 "PDF로 저장"이 있습니다.

**엑셀은 열 때만 내려받습니다.** 쓰지 않는 사람에게 400KB 를 미리 물리지 않으려고요.

---

## 자주 쓸 명령

```bash
cd /Users/motodna/Desktop/Rebind

# 로컬에서 보기 → http://localhost:8791/bindery.html
python3 -m http.server 8791

# 서버 함수 배포 (--no-verify-jwt 를 꼭)
supabase functions deploy share-view --no-verify-jwt --project-ref izrtclsqhsgkuwsffifn

# 부를 수 있는 주소 잠그기 (바꾼 뒤에는 다시 배포해야 반영됩니다)
supabase secrets set ALLOWED_ORIGIN="https://motodna.github.io,http://localhost:8791" \
  --project-ref izrtclsqhsgkuwsffifn
```

## 파일 구성

```
bindery.html                     앱 본체 (직원 화면 + 고객사 화면)
make-logo.py                     DNA Labs 마크를 그려 앱에 넣습니다
logo.png                         만들어진 마크 (180×180)
sql/0003_works.sql               표 · 접근 규칙 · 사진 보관함
supabase/functions/share-view/   고객사 공개 링크를 읽어 주는 서버 함수
supabase/functions/_shared/      어디서 부를 수 있는지 (Re:Call 것과 같은 파일)
supabase/project.json            접속 주소와 공개 키
설치방법.md                       설치 절차 10분
README.md                        무엇을 하는 앱인지
```
